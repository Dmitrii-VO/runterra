/**
 * Модуль территорий
 * 
 * Экспортирует интерфейсы, типы и DTO для работы с территориями.
 * На текущей стадии (skeleton) содержит только типы без бизнес-логики,
 * без репозиториев, сервисов и контроллеров.
 * 
 * ВАЖНО:
 * - Нет геометрии (полигоны, границы)
 * - Нет расчётов захвата
 * - Нет PostGIS
 * - Нет таймеров
 * - Нет сервисов
 * - Нет репозиториев
 * - Только контракты (interfaces, enums, DTO)
 * 
 * TODO для будущей реализации:
 * - Репозиторий для работы с БД
 * - Сервис с бизнес-логикой
 * - Контроллеры для API endpoints
 * - Валидация DTO через class-validator или zod
 * - Миграции БД для таблицы территорий
 * - Геометрия границ (PostGIS)
 * - Логика расчётов захвата
 */

export * from './territory.status';
export * from './territory.entity';
export * from './territory.dto';
