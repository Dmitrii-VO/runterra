/**
 * Типы событий
 * 
 * Описывает возможные типы событий в системе Runterra.
 * 
 * На текущей стадии (skeleton) содержит только enum без логики
 * и без бизнес-правил для различных типов событий.
 */

/**
 * Тип события
 */
export enum EventType {
  /** Тренировка */
  TRAINING = 'training',
  
  /** Совместный бег */
  GROUP_RUN = 'group_run',
  
  /** Клубное событие */
  CLUB_EVENT = 'club_event',
  
  /** Открытое событие */
  OPEN_EVENT = 'open_event',
}
