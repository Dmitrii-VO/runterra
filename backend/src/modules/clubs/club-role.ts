/**
 * Роли участников в клубе
 * 
 * Определяет роли пользователей в клубах системы Runterra.
 * 
 * На текущей стадии (skeleton) содержит только enum без логики.
 */

/**
 * Роль участника в клубе
 */
export enum ClubRole {
  /** Участник клуба */
  MEMBER = 'member',
  
  /** Модератор клуба */
  MODERATOR = 'moderator',
  
  /** Лидер клуба */
  LEADER = 'leader',
}
