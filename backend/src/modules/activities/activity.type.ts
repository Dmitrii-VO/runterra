/**
 * Типы активности
 * 
 * Описывает возможные типы тренировок/активностей в системе Runterra.
 * 
 * На текущей стадии (skeleton) содержит только enum без логики
 * и без бизнес-правил для различных типов активностей.
 */

/**
 * Тип активности
 */
export enum ActivityType {
  /** Бег */
  RUNNING = 'running',
  
  /** Ходьба */
  WALKING = 'walking',
  
  /** Велосипед */
  CYCLING = 'cycling',
  
  /** Тренировка (общий тип) */
  TRAINING = 'training',
}
