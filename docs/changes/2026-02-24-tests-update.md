# Обновление тестов (2026-02-24)

## Контекст
В связи с добавлением функционала Лидерборда и новых полей в тренировки (`target_value`, `target_zone`), требовалось обновить существующие тесты и добавить новые для обеспечения стабильности системы.

## Выполненные изменения

### 1. Backend (Jest)
- `backend/src/api/api.test.ts`: Добавлен новый блок тестов для `GET /api/clubs/leaderboard/:cityId`. Проверяется корректность возвращаемого списка лидеров и данных текущего клуба (очки, ранг).
- `backend/src/api/workouts.routes.test.ts`: Обновлены тесты создания (`POST`) и обновления (`PATCH`) тренировок. Теперь проверяется корректная передача и сохранение полей `targetValue` и `targetZone`.

### 2. Mobile (Flutter Test)
- Создан новый файл тестов `mobile/test/models/workout_test.dart` для проверки сериализации/десериализации (JSON) модели тренировки с новыми полями.
- Создан новый файл тестов `mobile/test/models/club_member_model_test.dart` для проверки корректности маппинга поля `totalDistance` (дистанция участника клуба).
- Создан новый файл тестов `mobile/test/models/city_leaderboard_entry_test.dart` для проверки парсинга данных лидерборда из API.

## Статус
Все 155 тестов бэкенда пройдены успешно (+1 новый тест). Все 28 тестов мобильного приложения пройдены успешно (+6 новых тестов моделей). Анализатор Flutter не выдает ошибок. Система стабильна.