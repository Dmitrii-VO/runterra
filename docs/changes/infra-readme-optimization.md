# Оптимизация infra/README.md (2026-02-12)

## Что сделано

- Секция `infra/README.md` с инфраструктурными деталями оставлена без изменений (сервер, БД, деплой, CI/CD).
- Блок `## TODO` очищен от выполненных пунктов:
  - В списке инфраструктуры оставлены только открытые задачи (Docker, автодеплой, staging, HTTPS/домен).
  - Для `Backend (важно)` и `Mobile` добавлены краткие пометки «открытых задач нет» с ссылкой на `docs/progress.md` и `docs/changes/*`.
  - Продуктовые фичи (тренировки, профиль, чаты, карта, клубы) заменены на короткое резюме с отсылкой в прогресс/changes.
- Раздел `### Ошибки` теперь содержит только реально открытые проблемы; для исправленных ошибок добавлена ссылка на `docs/errors-runbook.md` и `docs/progress.md`.
- Длинные исторические блоки (`Открытые фичи`, аудит, многочисленный feedback по backend/mobile) удалены из `infra/README.md` и заменены кратким разделом «История и аудит» с отсылкой в:
  - `docs/progress.md` — хронологический прогресс,
  - `docs/changes/*` — детализация по модулям.

## Зачем

- Сократить дублирование с `docs/progress.md` и `docs/changes/*` и убрать устаревшие TODO/статусы.
- Сделать `infra/README.md` компактной точкой входа по инфраструктуре и актуальным открытым задачам, а не свалкой всей истории.

