# Прогресс разработки

## Обзор
Документ отслеживает выполнение задач и прогресс разработки проекта Runterra.

## 2026-03-05 (продолжение 4)
- **Документация по последним коммитам (2026-03-05):** Зафиксирован свод изменений по коммитам `98e92eb..10c0aa4`: backend-валидации в событиях и назначениях тренеров, перенос создания тренерских групп в `ClubRosterScreen`, создание пустых групп и удаление группы с экрана редактирования, переход статистики профиля на `totalDistanceKm`, восстановление `statsTerritories` для CI-совместимости, авто-bump patch версии в `deploy-mobile.ps1`, автосинхронизация `APP_VERSION` на сервере и обновление NDK до `27.0.12077973`. Подробности — `docs/changes/2026-03-05-latest-commits-roundup.md`.

## 2026-03-05 (продолжение 3)
- **Состав клуба: группировка по тренерам + управление назначениями (2026-03-05):** Вкладка «Состав и планы» переработана. (1) **Backend:** добавлены 5 новых эндпоинтов в `clubs.routes.ts`. `GET /api/clubs/:id/trainer-assignments` — возвращает `{ currentUserRole, trainers: [{ trainerId, trainerName, personalClients, groups: [{ groupId, groupName, members }] }], unassigned }`, строит ответ из 3 SQL-запросов (все активные члены, trainer_clients отфильтрованный по членству, trainer_groups + trainer_group_members). `POST /api/clubs/:id/members/:userId/assign-trainer { trainerId }` — лидер назначает личного тренера (сначала удаляет все существующие личные тренера из данного клуба для участника, затем вставляет новую пару в trainer_clients). `DELETE /api/clubs/:id/members/:userId/assign-trainer` — убирает тренера. `POST /api/clubs/:id/members/:userId/assign-group { groupId }` — добавляет в группу. `DELETE /api/clubs/:id/members/:userId/assign-group/:groupId` — убирает из группы. Все операции только для лидера. `npm run build` — 0 ошибок. (2) **Mobile:** новая модель `trainer_assignment_model.dart` (`TrainerAssignmentsModel`, `TrainerEntry`, `TrainerGroupRef`, `MemberRef`). 5 новых методов в `ClubsService` (`getTrainerAssignments`, `assignTrainer`, `removeTrainer`, `assignGroup`, `removeFromGroup`). `ClubRosterScreen` полностью переработан: сгруппированный список по тренерам (секции-заголовки + подзаголовки групп), для лидера — тап на участника открывает bottom sheet с действиями (назначить/убрать тренера, добавить/убрать из группы), выбор через `SimpleDialog`. 10 новых L10n ключей (EN + RU). `flutter analyze` — 0 issues.

## 2026-03-05 (продолжение 2)
- **Fix: showCityPickerDialog в initState → crash при первом запуске (2026-03-05):** `_MapScreenState._ensureCityAndLoad()` вызывался напрямую из `initState`, и при отсутствии сохранённого города пытался открыть `showCityPickerDialog(context)` — но в `initState` контекст ещё не имеет доступа к Localizations. Это приводило к `dependOnInheritedWidgetOfExactType() called before initState() completed`. Исправлено: вызов `_ensureCityAndLoad()` обёрнут в `WidgetsBinding.instance.addPostFrameCallback`, что откладывает его до завершения первого build-прохода. Зоны (территории) на карте отображаются корректно — backend `/api/map/data?cityId=spb` возвращает 18 территорий.

## 2026-03-05 (продолжение)
- **График тренировок в Профиле (2026-03-05):** Добавлена секция «График тренировок» в `ProfileScreen` — месячный календарь с отметками завершённых пробежек и предстоящих событий. (1) **Backend:** `RunsRepository.getRunsForMonth(userId, year, month)` — выборка завершённых пробежек за месяц (UTC-границы). `EventsRepository.getRegisteredEventsForMonth(userId, year, month)` — события со статусом `registered`/`checked_in`, тип `!= 'training'`. Новый endpoint `GET /api/users/me/calendar?year=&month=` — возвращает `{ days: [{date, runs, events}] }`, только непустые дни, дата YYYY-MM-DD UTC. Валидация: year 2000–2100, month 1–12; при отсутствии параметров — текущий месяц. 4 новых теста (happy path, 401, invalid year, invalid month) — итого 160/160. (2) **Mobile:** модели `CalendarDayModel`, `CalendarRunEntry`, `CalendarEventEntry` в `calendar_model.dart`. Метод `UsersService.getCalendar(year, month)`. `TrainingCalendarWidget` — `StatefulWidget`, самодостаточный: навигация по месяцам (prev/next), заголовки дней недели через `DateFormat.E(locale)` (локаль-aware), точки-маркеры (🟢 run, 🔵 event), подсветка сегодняшнего дня, тап на день: один элемент → прямой переход, несколько → `showModalBottomSheet`. В bottom sheet `GoRouter.of(context)` захватывается до `Navigator.pop` — избегает навигации по невалидному context. Виджет добавлен в `ProfileScreen` перед `ProfileNotificationsSection`. 4 новых L10n ключа EN + RU (calendarTitle, calendarRun, calendarEvent, calendarChoose). `flutter analyze` — 0 issues. (3) **Bugfix deploy-scripts:** `deploy-all.ps1` и `deploy-backend.ps1` содержали UTF-8 em-dash (`—`), который Windows PowerShell 5.x читал как байт 0x94 (правая кавычка CP-1252) — ломал парсинг строк и выдавал каскадные ошибки скобок. Заменены на ASCII `-`.

## 2026-03-05
- **Wear OS: исправление багов и доработка модуля (2026-03-05):** Устранены 4 критических/средних бага и 3 улучшения модуля `wear/`. (1) **BUG-1:** `WatchService._broadcastRunState()` теперь вычисляет реальную длительность пробежки как `accumulatedDuration + (now - lastResumedAt)` вместо `session.duration` (которая равна 0 во время активной пробежки) — часы больше не показывают `00:00`. (2) **BUG-2:** `RunTrackingScreen._startRun()` вызывает `ServiceLocator.watchService.maybeResumeBroadcasting()` после старта — пробежка, начатая с телефона, теперь видна на часах. (3) **BUG-3:** В `wear/AndroidManifest.xml` добавлен `BODY_SENSORS_BACKGROUND` — ЧСС работает в ambient/background на Android 13+ / Wear OS 4+. (4) **BUG-4:** `WatchConnectivityService.init()` отправляет команду `getState` при старте часов; `WatchService` обрабатывает её — немедленно рассылает текущее состояние и запускает broadcasting если пробежка уже идёт. (5) **GAP-1:** `WatchService` принимает `CurrentClubService` через конструктор и передаёт `currentClubId` в `startRun()` — пробежки с часов теперь засчитываются территории. (6) **GAP-2:** `WatchShape` читается в `WatchHome.build()` и передаётся в экраны через параметр `isRound`; на круглых экранах padding 24–28px вместо 8px — контент не обрезается по углам. (7) **GAP-3:** В `WatchRunningScreen` в ambient-режиме при паузе отображается метка `PAUSED`. `flutter analyze` — 0 issues в `wear/` и `mobile/`.

## 2026-03-04 (продолжение 5)
- **Уведомление о новой версии приложения (2026-03-04):** Реализован диалог «Доступно обновление». (1) **Backend:** `GET /api/version` — публичный endpoint (до auth middleware), возвращает `{ latestVersion: string | null }` из env-переменной `APP_VERSION`. (2) **Mobile:** добавлены зависимости `package_info_plus` и `url_launcher`. Создан `VersionService` — читает текущую версию через `PackageInfo.fromPlatform()`, запрашивает последнюю с бэкенда, сравнивает по semver; проверка выполняется один раз за сессию. Создан `UpdateDialog` — отображает текущую и новую версии, кнопка «Обновить» открывает приложение Gmail (`googlegmail://`) с fallback на `https://mail.google.com`, кнопка «Закрыть» закрывает диалог. `RunterraApp` переведён с `StatelessWidget` на `StatefulWidget`; проверка версии вызывается через `addPostFrameCallback` при старте. (3) **L10n:** 6 новых ключей (EN + RU). (4) **Verification:** `flutter analyze` — 0 issues, `npm run build` — 0 errors.

## 2026-03-04 (продолжение 4)
- **Профиль: UX-оптимизация QuickActions и порядка секций (2026-03-04):** Рефакторинг вкладки Профиль. (1) `ProfileQuickActionsSection` переработана: убраны кнопки "Открыть карту" (дублировала таб) и "Найти людей" (не CTA); секция теперь скрывается (`SizedBox.shrink`) при `hasClub || isMercenary`; для новых пользователей без клуба показывает только два `OutlinedButton.icon` — "Найти клуб" и "Создать клуб". (2) В `ProfileScreen` `ProfileStatsSection` перемещена выше `ProfilePersonalInfoSection` — статистика важнее личных данных при первом взгляде. (3) "Найти людей" перенесено в `Card > ListTile` после Trainer Card — стилистически консистентно с остальными карточками-переходами. `flutter analyze lib/` — 0 issues.

## 2026-03-04 (продолжение 3)
- **Профиль: исправление nextActivity/lastActivity + редизайн ProfileScreen (2026-03-04):** (1) **Backend:** устранена заглушка `nextActivity: undefined, lastActivity: undefined` в `GET /api/users/me/profile`. Добавлен метод `EventsRepository.getNextEventForUser(userId)` — возвращает ближайшее будущее событие, на которое зарегистрирован пользователь (JOIN event_participants + events, фильтр `start_date_time > NOW()`, статусы `registered`/`checked_in`). Добавлен `RunsRepository.getLastRun(userId)` — последняя пробежка пользователя. Оба метода замокированы в `__mocks__/index.ts`. Все 156 тестов зелёные. (2) **Mobile:** полный редизайн `ProfileScreen` — замена `Scaffold + AppBar + ListView` на `CustomScrollView` с `SliverAppBar` (`expandedHeight: 200`, `pinned: true`). Новый приватный виджет `_ProfileHeroHeader`: аватар (36px radius), имя, статус-чип поверх градиента основного цвета темы. Карточка-заголовок (`ProfileHeaderSection`) убрана из скролла, так как переехала в `flexibleSpace`. `flutter analyze` — 0 errors. Подробности — docs/changes/2026-03-04-profile-screen-redesign.md.

## 2026-03-04 (продолжение 2)
- **Тренерская система: отслеживание выполнения заданий и назначение группе (2026-03-04):** Закрыты критические пробелы тренерской логики P0/P1. (1) **DB:** миграция `038_runs_assignment_id.sql` — `runs.assignment_id UUID REFERENCES workout_assignments(id) ON DELETE SET NULL`; индекс по `assignment_id`. Связь опциональная (клиент может бегать без задания). (2) **Backend:** `Run` entity + `RunRow` + `rowToRun` расширены полем `assignmentId`. `RunsRepository.findByClientForTrainer(trainerId, clientId)` — выборка завершённых пробежек клиента с JOIN на workout_assignments и workouts (название задания). `WorkoutsRepository.findAssignedToUser` — добавлены `assignmentId` и `isCompleted` (EXISTS-подзапрос: есть ли runs с этим assignment_id у данного клиента). `WorkoutsRepository.assignToClients(workoutId, trainerId, clientIds, note)` — batch-назначение нескольким клиентам. `GET /api/trainer/clients/:clientId/runs` — тренер видит пробежки своего клиента (RPE, notes, дистанция, длительность, связанное задание). `POST /api/workouts/:id/assign-group` — batch-назначение тренировки всей группе (проверяет: автор workout == caller, group.trainerId == caller, группа непустая). 156 тестов зелёные. (3) **Mobile:** `AssignedWorkout` модель расширена `assignmentId` + `isCompleted`; assigned-tab в `WorkoutsListScreen` показывает иконку ✓ (зелёная) / ○ (серая). Новая модель `ClientRunModel` и экран `ClientRunsScreen` — список пробежек клиента с дистанцией, длительностью, RPE, заметками, ссылкой на название задания. В `DirectChatScreen` добавлена кнопка 📊 в AppBar (только для тренера) → `/trainer/clients/:clientId/runs`. `WorkoutsService.assignWorkoutToGroup()`. `_showAssignDialog` переработан: two-tab bottom sheet «Клиент / Группа». `TrainerService.getClientRuns()`. Новый роут `/trainer/clients/:clientId/runs`. 7 новых i18n ключей (EN + RU). `flutter analyze` — 0 issues. (4) **Deploy:** скрипт `deploy-all.ps1` исправлен — Wear OS теперь пропускается через `git diff origin/main..HEAD -- wear/` вместо ненадёжного `git describe --tags`.

## 2026-03-04 (продолжение)
- **Система назначения тренировок тренером → клиенту (2026-03-04):** Реализован полный цикл «тренер назначает шаблон → клиент видит список назначенных тренировок». (1) **DB:** миграция `037_workout_assignments.sql` — таблица `workout_assignments` (FK на workouts, trainer_id, client_id, note, assigned_at, UNIQUE по workout+client), индексы по client_id и trainer_id. (2) **Backend Fix:** `POST /api/trainer/clients/:userId` теперь разрешает добавление клиента для тренера с `acceptsPrivateClients=true` без обязательного shared-клуба (обратная совместимость через shared-club fallback). (3) **Backend New:** в `WorkoutsRepository` добавлены методы `assignToClient`, `unassignFromClient`, `findAssignedToUser`. `GET /api/workouts?assigned=true` возвращает назначенные тренером шаблоны с полями `trainerId`, `trainerName`, `note`, `assignedAt`. `POST /api/workouts/:id/assign` — назначает шаблон клиенту (только автор + проверка trainer_clients). `DELETE /api/workouts/:id/assign/:clientId` — снимает назначение. Mock обновлён для тестов. (4) **Mobile:** новая модель `AssignedWorkout extends Workout`; в `WorkoutsService` — методы `getAssignedWorkouts`, `assignWorkout`, `unassignWorkout`; `WorkoutsListScreen` расширен третьей вкладкой «От тренера» с пустым состоянием; на личных тренировках — иконка-кнопка «Назначить клиенту» (person_add) с bottom sheet выбора клиента; 10 новых i18n ключей (EN + RU). `npm run build` — 0 ошибок, 156 тестов — зелёные, `flutter analyze` — 0 issues.

## 2026-03-04
- **Fix: удаление тренировки → 500 (FK violation) (2026-03-04):** При нажатии «Удалить» на тренировке, связанной с прошедшими событиями, возвращался 500 Internal server error. Причина: `hasUpcomingEvents` проверяет только будущие события, но FK-констрейнт `events_workout_id_fkey` блокирует удаление при наличии любых событий (включая завершённые). В catch-блоке DELETE-обработчика `workouts.routes.ts` добавлена проверка PG-кода `23503` → 409 `workout_in_use`. Подробности — docs/changes/2026-03-04-workout-simplify.md.
- **Упрощение формы тренировок (2026-03-04):** Полностью переработана форма создания/редактирования тренировок. (1) **DB:** миграция `035_workout_simplify.sql` — обновлён CHECK-констрейнт типов (4 новых: FUNCTIONAL, TEMPO, RECOVERY, ACCELERATIONS), добавлены 7 новых колонок (`distance_m`, `heart_rate_target`, `pace_target`, `rep_count`, `rep_distance_m`, `exercise_name`, `exercise_instructions`), мигрированы существующие данные (INTERVAL→ACCELERATIONS, FARTLEK→TEMPO, LONG_RUN→RECOVERY). Старые поля (difficulty, surface, blocks) оставлены в БД. (2) **Backend:** обновлены `workout.type.ts` (4 новых enum-значения), `workout.entity.ts` (добавлены 7 полей), `workout.dto.ts` (обновлена Zod-схема, difficulty/targetMetric стали опциональными с дефолтами), `workouts.repository.ts` и `workouts.routes.ts` (передача новых полей в INSERT/UPDATE). (3) **Mobile:** `workout.dart` — упрощена модель (убраны blocks, targetMetric и т.д., добавлены 7 новых полей); `workouts_service.dart` — обновлён `createWorkout`; `workout_form_screen.dart` — полная замена блочного редактора на упрощённую форму с секцией полей, зависящей от выбранного типа (TEMPO/RECOVERY: дистанция, пульс, темп; ACCELERATIONS: длина отрезка, кол-во повторений, темп; FUNCTIONAL: упражнение, инструкции, повторения); `workouts_list_screen.dart` и `event_details_screen.dart` — обновлён `_localizeType` для новых типов. (4) **L10n:** добавлены 9 новых ключей (EN + RU), удалены typeInterval/typeFartlek/typeLongRun. (5) Побочные исправления: блок-трекинг в `run_service.dart` заменён на no-op (blocks убраны из модели), `run_tracking_screen.dart` упрощён до отображения только имени тренировки. `npm run build` — 0 ошибок, 155 тестов — зелёные, `flutter analyze` — 0 errors.

## 2026-03-02
- **Поиск людей (Find People) (2026-03-02):** Реализован минимальный инструмент поиска бегунов в своём городе. (1) **Backend:** `GET /api/users/search` — поиск по имени (ILIKE, мин 2 симв), фильтр по cityId, пагинация (limit/offset), исключение текущего пользователя и скрытых профилей; метод `UsersRepository.searchByName()` с JOIN на club_members; `UserSearchResultDto` с полями id, name, firstName, lastName, avatarUrl, cityId, cityName, clubName. Маршрут зарегистрирован до `/:id`. (2) **Mobile:** модель `UserSearchResult`; `UsersService.searchUsers()` + `getRawUserById()`; `PeopleSearchScreen` (дебаунс 400ms, фильтр «Мой город», пагинация «Загрузить ещё»); `PublicProfileScreen` (name, avatar, город/клуб из search result, disabled-кнопка «Написать»); 2 новых роута `/people` и `/user/:id`; кнопка «Найти людей» в Quick Actions на экране профиля. 6 новых i18n ключей (EN + RU). `npm run build` — 0 ошибок, `flutter analyze` — 0 issues.

## 2026-02-28
- **Динамическая навигация (Скрытие вкладок) (2026-02-28):** Реализована логика адаптивного интерфейса: вкладки «Сообщения» и «События» скрываются для новых пользователей без клубов/тренеров. (1) Backend: эндпоинт `GET /api/users/me/nav-status` и метод `getNavigationStatus` в `UsersRepository` (проверка `club_members`, `trainer_groups`, `trainer_clients`). (2) Mobile: `UserNavStatusNotifier` (ValueNotifier) управляет состоянием видимости; `BottomNav` фильтрует `items` и корректно сопоставляет индексы с ветками `GoRouter`. (3) Architecture: обновлена карта `docs/domain_map.html`, добавлен узел логики навигации. Подробности — docs/changes/2026-02-28-dynamic-navigation.md.
- **Fix: Timezone отображения событий (2026-02-28):** Исправлен баг, при котором время события отображалось в UTC вместо локального времени устройства. (1) В `event_card.dart` (events и map) и `event_details_screen.dart` добавлен вызов `.toLocal()` перед форматированием даты. (2) К отображаемому времени добавлено название timezone устройства (`timeZoneName`) — например «28.2.2026 15:30 МСК». (3) Исправлен баг в `events_service.dart` `updateEvent` — при редактировании события `startDateTime` теперь конвертируется в UTC через `.toUtc()` (аналогично `createEvent`). `flutter analyze` — 0 errors.

- **Профессиональный конструктор тренировок (Athletic Pro) (2026-02-28):** Внедрена структура тренировок на основе блоков (jsonb), сегментов и типов покрытия (surface). (1) `mobile/lib/shared/models/workout.dart` и `backend` переведены на новую модель. (2) В `WorkoutFormScreen` добавлен UI-редактор с поддержкой времени/дистанции, зон RPE и видео-инструкций. (3) `RunTrackingScreen` научился отслеживать шаги тренировки, добавлены звуковые оповещения через `flutter_tts` при смене сегмента. (4) В отчет о пробежке добавлены поля `RPE` и `заметки для тренера`. Подробности — docs/changes/2026-02-28-athletic-pro.md.
- **Исправление редактирования профиля тренера (2026-02-28):** Исправлен баг, при котором экран редактирования тренера открывался пустым при наличии профиля. (1) `ProfileScreen` теперь сохраняет объект `TrainerProfile` и передает его в `/trainer/edit`. (2) `TrainerEditProfileScreen` автоматически загружает профиль текущего пользователя, если он не был передан (защита от пустых полей). (3) В `TrainerProfileScreen` добавлена кнопка редактирования в AppBar для владельца профиля. `flutter analyze` — 0 errors. Подробности — docs/changes/2026-02-28-trainer-profile-edit-fix.md.
- **Перенос функционала «Найти тренера» (2026-02-28):** Точка входа «Найти тренера» перенесена из раздела «Профиль» во вкладку «Сообщения -> Тренер» для улучшения UX. (1) Из `ProfileScreen` удалена карточка поиска тренеров. (2) В `CoachTab` добавлена постоянная кнопка поиска (Icons.search) в верхней части рядом с переключателем вложенных вкладок. (3) В пустом состоянии вкладки «Личные» (когда тренера ещё нет) для атлетов добавлена кнопка `ElevatedButton` «Найти тренера», ведущая в каталог `/trainers`. `flutter analyze` — 0 errors. Подробности — docs/tasks/2026-02-28-relocate-find-trainer.md.
- **Fix: HTTP 401 в Профиле (2026-02-28):** Исправлено отображение технической ошибки 401 Unauthorized в личном кабинете. (1) В `ProfileScreen` добавлена логика автоматического обновления токена при получении 401. (2) В случае повторной неудачи или истечения сессии теперь отображается понятный экран ошибки с заголовком «Ошибка авторизации» и кнопкой «Войти заново», которая выполняет `signOut` и перенаправляет на экран логина. (3) Добавлены соответствующие ключи локализации в `app_ru.arb` и `app_en.arb`.
- **Fix: Исчезновение блока тренера в Профиле (2026-02-28):** Исправлен баг, при котором карточки тренера пропадали при обновлении страницы из-за состояния гонки (race condition) и истечения токена. Теперь загрузка статуса тренера (`_loadTrainerProfile`) строго дожидается успешного завершения основного запроса профиля (`_fetchProfile`), который гарантирует наличие актуального токена.
- **CI: кэширование pub и Gradle для mobile build (2026-02-28):** В `.github/workflows/ci.yml` добавлено кэширование зависимостей: `pub cache` (ключ по pubspec.lock) и Gradle (ключ по gradle-wrapper + build.gradle). Ускоряет повторные сборки Mobile job. Подробности — docs/changes/2026-02-28-ci-mobile-cache.md.
- **Миграция open_event и правки mobile (2026-02-28):** (1) Backend: миграция `029_events_type_open_event.sql` — расширен CHECK для `events.type`, добавлено значение `open_event` (устранена check_violation при вставке). (2) Mobile: в `app.dart` убрана автоматическая подстановка вызова `ServiceLocator.refreshAuthToken` при проверке маршрутов (избежание лишних запросов). (3) `LocationPickerScreen`: результат `Navigator.pop` приведён к строго типизированному `Map<String, double>` (latitude, longitude); поле `address` убрано из возвращаемого payload. Подробности — docs/changes/2026-02-28-open-event-mobile-fixes.md.
- **Создание события с начальным типом и маркеры по zoom (2026-02-28):** (1) `CreateEventScreen` принимает начальный тип события через query-параметр (`/event/create?type=training` или `type=open_event`); маршрут создания с вкладки «События» передаёт тип в зависимости от контекста (тренировка / открытое событие). (2) На карте (`MapScreen`) маркеры событий отображаются условно в зависимости от уровня zoom — улучшение читаемости при большом охвате. Подробности — docs/changes/2026-02-28-event-creation-map-ux.md.
- **Кастомные стили карты (2026-02-28):** Добавлена утилита `mobile/lib/shared/utils/map_style.dart` с общим стилем карты. Стиль подключён к `LocationPickerScreen` и `MapScreen` — при создании/просмотре карта отображается в едином визуальном стиле. Затем из стиля убрана избыточная конфигурация point style для business tags (refactor) для упрощения кода. Подробности — docs/changes/2026-02-28-map-styles.md.
- **Вкладка «Тренер» в Сообщениях — полная реализация (2026-02-28):** Реализовано двустороннее общение тренер↔ученики. (1) **Backend:** миграция `030_trainer_direct.sql` — таблицы `trainer_clients` (связь тренер↔клиент) и `direct_messages` (1:1 сообщения). Новые endpoints: `POST/DELETE /api/trainer/clients/:userId` (управление клиентами), `GET /api/messages/trainer/clients` (список клиентов тренера), `GET /api/messages/trainer/my-trainer` (тренер текущего пользователя), `GET/POST /api/messages/direct/:otherUserId` (история и отправка 1:1 сообщений). В `GET /api/messages/clubs/:clubId` добавлено поле `senderRole` (JOIN с club_members). WebSocket поддерживает `direct:{id1}:{id2}` каналы. (2) **Mobile:** модель `DirectChatModel`, поле `senderRole` в `MessageModel`. В `ClubMessagesTab` добавлен параметр `highlightTrainer` — бейдж «Тренер» рядом с именем в чате. Новый `DirectChatScreen` — 1:1 чат с WS + polling fallback, блокировка ввода для клиента до первого сообщения тренера. `ClubDetailsScreen`: расширен тап по участнику — action sheet с опциями «Написать как тренер» / «Изменить роль» / «Личные сообщения (в разработке)». `CoachTab` полностью переписан: вложенные вкладки «Группы» (ClubMessagesTab с highlightTrainer) и «Личные» (список клиентов для тренера, чат с тренером для атлета). 11 новых i18n ключей. 155 backend тестов, flutter analyze — 0 errors. Подробности — docs/changes/2026-02-28-trainer-messaging.md.

## 2026-02-26
- **Выбор клуба для пробежки и фикс багов карты (2026-02-26):** (1) В модель `RunSession` добавлено поле `scoringClubId`. (2) `RunService` переработан: `startRun` принимает `scoringClubId`, он сохраняется в сессии и отправляется в `submitRun` как фоллбэк. (3) На экране `RunTrackingScreen` перед началом пробежки добавлена карточка и диалог выбора клуба, загружающий список клубов пользователя (`ServiceLocator.clubsService.getMyClubs`) и подставляющий активный клуб по умолчанию. (4) Исправлен баг множественного открытия окна территории на карте (`MapScreen`) путем введения флага блокировки `_isSheetShowing`. Подробности — docs/changes/2026-02-26-territory-run-club-selection.md.
- **Верификация Feedback-фиксов (2026-02-25):** Проверены все 8 пунктов реализации. (1) Карта: подтверждено отсутствие двойной анимации. (2) Тренировка: подтверждена кнопка «←» в AppBar. (3) Сообщения: подтверждена работа флага `_userScrolledAway`. (4) Сообщения: кнопка «↓» проверена и работает. (5) Баннер активного клуба: проверена логика Timer и PIP. (6) Создание события: **исправлено** (удалены `group_run` и `club_event` из дропдауна). (7) Вкладка «События»: фильтр `open_event` подтвержден. (8) FAB тренировок: логика видимости по ролям подтверждена.
- **Исправление Gradle и Java 21 (CI):** Обновлен Gradle до `8.10.2` для `mobile` и `wear` модулей. Исправлены несовместимости с Flutter 3.24.5 (`withOpacity`, `value`). Добавлена очистка `gradle.properties` в CI. Исправлены ошибки типизации в `app.dart`. Подробности — docs/changes/2026-02-26-fix-gradle.md.
- **Инфраструктура:** Backend успешно обновлен на сервере (v0.1.0+).

## 2026-02-25
- **Feedback-фиксы (2026-02-25):** Реализованы 8 фидбеков по мобильному приложению. (1) Карта: убрана двойная анимация центрирования (`_centerMapOnStartPosition` из `_loadMapData` удалена, оставлена только в `_onMapCreated` с `duration: 0.0`). (2) Тренировка: добавлена кнопка «←» в AppBar при `_state == idle` и наличии `onRunCompleted` callback. (3) Сообщения: добавлен флаг `_userScrolledAway` — входящие не скроллят список при чтении истории; кнопка «↓» (FloatingActionButton.small) появляется когда пользователь отскроллил >200px от дна. (4) Карта + Тренировка: баннер активного клуба и текущей территории (point-in-polygon и haversine для кругов, Timer.periodic каждые 30с, выбор клуба через bottom sheet при нескольких клубах). (5) Создание события: дропдаун оставлен с 2 вариантами: «Тренировка» и «Открытое событие» (убраны `group_run` и `club_event`). (6) Вкладка «События»: только `open_event` в списке (client-side фильтр). (7) FAB на вкладке «Тренировки»: скрыт от обычных пользователей, виден только тренерам и лидерам. Добавлено 6 ключей l10n (EN + RU). `flutter analyze` — 0 errors, 35 info (pre-existing). Подробности — docs/changes/2026-02-25-feedback-fixes.md.

- **Deploy Wear OS через npm run deploy (2026-02-24):** Добавлен шаг деплоя Wear OS в скрипт `npm run deploy`. Создан `scripts/deploy-wear.ps1` — собирает APK в `wear/` и загружает в Firebase App Distribution только если с последнего git-тега есть изменения в `wear/` (поддерживает `-Force` и `-SkipFirebase`). `scripts/deploy-all.ps1` вызывает wear-деплой после mobile. В `scripts/app-distribution.config.json` добавлено поле `firebaseWearAppId` (пустое — скрипт выводит инструкцию по настройке Firebase). Подробности — docs/changes/2026-02-24-deploy-wear.md.
- **Поддержка Wear OS (2026-02-24):** Реализован MVP-мост между телефоном и умными часами. Создан отдельный Flutter-проект `wear/` с двумя экранами (WatchIdleScreen, WatchRunningScreen), сервисом связи `WatchConnectivityService` (watch_connectivity) и нативным плагином `HeartRatePlugin.kt` для чтения ЧСС с датчика часов. На стороне телефона: `WatchService` слушает команды часов (start/pause/resume/stop/hr), вызывает `RunService`, транслирует состояние пробежки на часы каждые 5 сек. `RunSession` расширена полем `heartRate: int?`. Все существующие конструкторы `RunSession` сохраняют heartRate. `RunTrackingScreen` показывает ЧСС в реальном времени (во время пробежки) и на экране результатов. `WatchService` зарегистрирован в `ServiceLocator`. Добавлены зависимость `watch_connectivity: ^0.2.8` и i18n-ключ `watchNotPaired`. Созданы ADR-0009 и docs/changes. flutter analyze — 35 info (pre-existing), 0 errors. Подробности — docs/changes/2026-02-24-wear-os-support.md.
- **Обновление тестов (2026-02-24):** Добавлены и обновлены тесты для Лидерборда и новых полей тренировок. На бэкенде протестирован эндпоинт `GET /api/clubs/leaderboard/:cityId` и сохранение `targetValue/targetZone`. На мобильном клиенте созданы тесты для моделей `Workout`, `ClubMemberModel` и `CityLeaderboardEntry`. Все 155 бэкенд-тестов и 28 мобильных тестов проходят. Подробности — docs/changes/2026-02-24-tests-update.md.
- **Кнопка удаления тренировки (2026-02-24):** На экран редактирования тренировки (`WorkoutFormScreen`) добавлена явная кнопка «Удалить» с диалогом подтверждения. Реализована обработка ошибок удаления (например, блокировка при наличии привязанных будущих событий). Добавлены новые ключи локализации. Подробности — docs/changes/2026-02-24-workout-delete-button.md.
- **Добавление целевых значений в тренировки (2026-02-24):** Расширена модель тренировок (Workout) для поддержки конкретных целевых показателей и пульсовых зон. Добавлены поля `target_value` (например, 5000 метров) и `target_zone` (например, 'Z2'). На мобильном клиенте в `WorkoutFormScreen` реализован динамический ввод значения (лейбл зависит от выбранной метрики) и дропдаун для выбора зоны (Z1-Z5). Добавлены соответствующие миграции БД, обновлены репозитории и локализация. Подробности — docs/changes/2026-02-24-workout-target-fields.md.
- **Рейтинг клубов в городе (Лидерборд) (2026-02-24):** Реализован MVP-рейтинг клубов. Добавлен бэкенд-эндпоинт `GET /api/clubs/leaderboard/:cityId`, вычисляющий топ-10 клубов города (очки = участники × 1 + территории × 10). `ClubDetailsScreen` теперь по нажатию на метрику "Рейтинг в городе" открывает Bottom Sheet с реальным списком лидеров (и вашим клубом). Добавлены локализации для "Pts" и "Members, Territories". Подробности — docs/changes/2026-02-24-city-leaderboard.md.
- **Улучшения UI деталей клуба и логики территорий (2026-02-24):** Переработан экран `ClubDetailsScreen`. Блоки «Основатель» и «Участники» убраны из основного скролла. Метрики (Участники, Территории, Рейтинг) стали кликабельными. Нажатие на «Участники» открывает Bottom Sheet со списком, где теперь также отображается суммарный прогресс (в км) каждого участника (с учетом старых пробежек без `scoring_club_id`). Нажатие на «Территории» открывает Bottom Sheet со списком захваченных и оспариваемых территорий клуба с указанием прогресса и отрыва от лидера, с переходом в детали территории. Статусы территорий и город локализованы на русский язык. Устранены ошибки сборки CI. Подробности — docs/changes/2026-02-24-club-details-ui-metrics-and-territories.md.
- **Тренер: приватный / клубный / участник (2026-02-24):** Реализована модель «два независимых аспекта» (TrainerProfile + clubRole). (1) **DB:** миграция `027_trainer_private.sql` — `ALTER TABLE trainer_profiles ADD COLUMN accepts_private_clients BOOLEAN NOT NULL DEFAULT false`. (2) **Backend entity/DTO:** `acceptsPrivateClients` добавлено в `TrainerProfile`, `CreateTrainerProfileSchema`, `UpdateTrainerProfileSchema`. (3) **Репозиторий:** `trainer_profiles.repository.ts` обновлён — `findPublicTrainers(filters)` (JOIN с users, фильтры по cityId/specialization). (4) **trainer.routes.ts:** убран гейт `isTrainerInAnyClub()` из POST/PATCH (теперь любой пользователь с bio+specialization может создать профиль); добавлен `GET /api/trainer` — discovery endpoint (accepts_private_clients=true). (5) **events.routes.ts:** добавлена авторизация в `POST /api/events`: club events → `isTrainerOrLeaderInClub(userId, organizerId)`; trainer events → проверка `accepts_private_clients=true` + `organizerId===userId`. (6) **Mobile:** модель `TrainerProfile` + новый `PublicTrainerEntry`; `TrainerService.getTrainers()`; toggle `acceptsPrivateClients` в `TrainerEditProfileScreen`; badge «Приватный тренер» в `TrainerProfileScreen`; секция «Тренер» в `EditProfileScreen` (toggle + кнопка настройки); тренерская карточка в `ProfileScreen` скрыта если нет TrainerProfile; новый экран `TrainersListScreen` (discovery) с роутом `/trainers` и фильтром по специализации; 8 новых i18n ключей в обоих ARB. Backend: 154 теста зелёных. Flutter analyze — 0 errors. Подробности — docs/changes/2026-02-24-trainer-private-clients.md.

- **Fix: чат всегда скроллит к отправленному своему сообщению (2026-02-24):** В `_sendMessage()` (`club_messages_tab.dart`) убрано условие `wasNearBottom` — теперь `_scrollToBottomDeferred(animated: true)` вызывается безусловно после отправки своего сообщения. Входящие чужие сообщения скроллят только если пользователь и так внизу (без изменений). flutter analyze — 0 issues.
- **Fix: несоответствие адрес ≠ координаты в пикере локации (2026-02-24):** Три взаимосвязанных бага: (A) `_onSuggestItemSelected` устанавливал `_selectedAddress` даже для подсказок без координат → адрес не совпадал с пином; (B) ручное перетаскивание карты не сбрасывало `_selectedAddress` → старый адрес оставался; (C) при редактировании события адрес из пикера не обновлял поле «Название локации» если оно было непустым. Фиксы: A — `_onSuggestItemSelected` игнорирует элементы с `center == null`; B — `_onCameraPositionChanged` сбрасывает `_selectedAddress` при `reason == gestures`; C — оба экрана (create/edit) теперь всегда перезаписывают поле адреса. UX: добавлен адрес под координатами в нижней карточке пикера. flutter analyze — 31 info (pre-existing), 0 errors.
- **Fix: создание события вне границ города (2026-02-24):** Ошибка `validation_error: Request body validation failed` при выборе точки юж. Пушкина/Гатчины. Причина: `SPB_BOUNDS.sw.latitude=59.7` отрезал юг области. Расширены границы СПб: SW `29.0/59.4`, NE `31.0/60.3`. Mobile: `events_service.createEvent` теперь парсит `details.fields[0].code` и выставляет его как `ApiException.code`; `CreateEventScreen` показывает `eventCreateLocationOutOfCity` при коде `coordinates_out_of_city` вместо сырого сообщения. flutter analyze — 0 issues.
- **Датапикер «Тренировки»: цветовая индикация (2026-02-24):** Улучшен `_buildCalendarStrip()` в `events_screen.dart`. Добавлены: заголовок с текущим месяцем/годом; 4 визуальных состояния ячейки (выбрана/сегодня/прошедшая/будущая); цветные точки-индикаторы под числом (синяя = event, оранжевая = note) через `_loadedCalendarItems` state и хелперы `_buildDots`/`_dot`; иконка пустого состояния заменена на `calendar_today_outlined`. flutter analyze — 0 issues.
- **Fix CI: Mobile analyze (2026-02-20):** CI падал на Mobile job, шаг «Analyze code»: 2 error в `test/models/event_list_item_model_test.dart` — обращение к `event.startLocation.latitude/longitude` при nullable `startLocation`. Исправлено: `event.startLocation!.latitude` / `event.startLocation!.longitude`. Удалён неиспользуемый `_backendDayToUi` в `club_schedule_screen.dart`. `flutter analyze --no-fatal-infos` выходит с 0.
- **Fix CI: GET /api/activities тест (2026-02-20):** CI падал на тесте «GET /api/activities › returns 200 with array» (ожидался 200, получали 500). Причина: в моке репозиториев не было `getActivitiesRepository`/`mockActivitiesRepository` — при вызове роута репозиторий возвращал undefined, `activities.map` падал. Добавлены `mockActivitiesRepository` (findByUserId → [], findById → null, create → stub) и `getActivitiesRepository` в `backend/src/db/repositories/__mocks__/index.ts`. Все 152 теста проходят.
- **Fix CI/deploy (2026-02-20):** Деплой падал из-за неуспешного CI. Исправлены ошибки backend (TS): добавлен импорт getEventsRepository в clubs.routes; planType добавлен в ClubMembershipRow и маппинг в club_members.repository; путь к schedule.dto в schedule.repository исправлен на ../../modules/schedule; в Event добавлено isManuallyEdited, в events.repository — EventRow.is_manually_edited, rowToEvent, create(workoutId, trainerId). В mobile: AppBar.subtitle заменён на Column в personal_schedule_screen (subtitle не существует в AppBar); удалены неиспользуемые импорты в club_roster_screen и club_schedule_screen; в schedule_model — doc comment на //. Push выполнен, CI Backend прошёл; деплой ожидает завершения CI Mobile (Build APK) в фоне.
- **Проверка тестов (2026-02-19):** Выполнена полная проверка backend (147 тестов) и mobile (22 теста) — все проходят. devLogClient: в NODE_ENV=test отключена отправка логов на dev-сервер (исключает возможные утечки fetch при прогоне тестов). Подробности — docs/changes/2026-02-19-tests-verification.md.
- **Fix: async file write in logger (2026-02-19):** `backend/src/shared/logger.ts` — заменён `fs.appendFileSync` на `fs.appendFile` (async callback) в `writeToFile`. Устранено блокирование Event Loop при записи логов. Комментарий в коде теперь соответствует реализации. Catch-блок обновлён: теперь явно сообщает об ошибке инициализации директории, а не записи. Build clean, 147 тестов зелёные.
- **Deploy fixes (2026-02-18):** Исправлены блокирующие деплой ошибки: восстановлены pubspec, добавлены temp-файлы в gitignore, Flutter понижен до 3.24.5, добавлен патч yandex_mapkit (toARGB32→value), Color API приведён к совместимости с 3.24, Yandex MapKit обновлён до 4.22.0-lite. Backend успешно задеплоен. Mobile Build в CI всё ещё падает на Android-зависимостях MapKit (cannot access Point/MapKitFactory). Подробности — docs/changes/2026-02-18-deploy-fixes.md.
- **Реальный захват территорий по GPS + приватные пробежки (2026-02-17):** Backend: добавлен реальный подсчёт вклада по GPS-треку пробежки в территории (таблицы `territory_*`, транзакции, `scoringClubId`), а также приватность событий (`visibility=private` скрывает событие из фида/карты и закрывает доступ не-участникам). Mobile: выбор клуба для зачёта при нескольких клубах (по ошибке `club_required_for_scoring`) и переключатель приватности при создании события. Детали — `docs/changes/2026-02-17-territory-capture-implementation.md` и `docs/changes/2026-02-17-territory-capture-audit-fixes.md`.
- **Спека: Реальный захват территорий по GPS + приватные group runs (2026-02-17):** Оформлен план перехода от mock-территорий к реальному начислению очков по GPS-треку пробежек: начисление только active-участникам клубов, выбор клуба для начисления после завершения пробежки, сезон = месяц (как в текущем UX). Также описано добавление приватных `group_run` (не на карте/в списке, join по ссылке, видимость только участникам). Документ: `docs/territory-capture-real-gps-implementation-spec.md`. Связанный документ меркателей обновлён: `docs/changes/mercenaries-requirement.md` (MVP: безклубные пробежки не влияют).
- **Фикс: myClubProgress null для реальных клубов (2026-02-17):** В `GET /api/territories/:id` mock-лидерборд содержит фейковые clubId (`mock-club-*`), а реальные пользователи состоят в клубах с UUID — совпадение невозможно, `myClubProgress` всегда null. Фикс: если пользователь в клубе, но его нет в лидерборде — создаётся синтетическая запись (position = last+1, totalKm = 0, gapToLeader = -leaderKm) и добавляется в leaderboard. Теперь bottom sheet показывает Battle Progress и «Run for Zone» вместо дублированного «Найти клуб». Файл: `backend/src/api/territories.routes.ts`. Подробности — docs/changes/territory-capture-mobile.md.
- **Аудит-фиксы: захват территорий + targetMetric (2026-02-17):** (1) Mobile: добавлен `TerritoriesService.captureTerritory()` — POST на `/api/territories/:id/capture`. (2) TerritoryBottomSheet: добавлена кнопка захвата (Icons.flag) в action bar между "Run for Zone" и leaderboard; показывается только при наличии клуба; loading state + snackbar (успех/ошибка). (3) WorkoutsListScreen: `Row` → `Wrap`, добавлен третий Chip с targetMetric (Distance/Time/Pace); добавлен метод `_localizeTargetMetric`. (4) i18n: 3 ключа (captureButton, captureSuccess, captureError) в оба ARB файла. TerritoryDetailsScreen не менялся (основной UX — bottom sheet). Flutter analyze — 0 errors, flutter test — 22 passed, backend tests — 139 passed. Подробности — docs/changes/territory-capture-mobile.md.
- **Prod bug — Trainer profile edit 500 (2026-02-17):** В прод-логах `GET /api/trainer/profile/edit` → 500 (`22P02 string_to_uuid`). Причина: `/api/trainer/profile/:userId` вызывается с `userId=edit` (часто из-за матчинга маршрута `/trainer/edit` как `/trainer/:userId`). Требуется: UUID-валидация `:userId` на backend и порядок/ограничение маршрутов в mobile. Исправление в коде: mobile перестановка роутов (/trainer/edit перед /trainer/:userId) + backend UUID-валидация :userId (400 вместо 500).
- **Trainer 2.0 — полная реализация (2026-02-16):** Реализован функционал тренера согласно `docs/trainer-implementation-spec.md`. (1) **Backend миграции:** 3 новых — `018_trainer_profiles.sql` (таблица trainer_profiles: bio, specialization[], experience_years, certificates JSONB), `019_workouts.sql` (таблица workouts: author_id, club_id, name, description, type, difficulty, target_metric + индексы), `020_events_trainer_fields.sql` (ALTER events + workout_id, trainer_id). (2) **Backend модули:** `src/modules/trainer/` (entity, Zod DTO), `src/modules/workout/` (entity, enums WorkoutType/Difficulty/TargetMetric, Zod DTO). Расширены `event.entity.ts` и `event.dto.ts` полями workoutId, trainerId, workoutName, trainerName. (3) **Репозитории:** `trainer_profiles.repository.ts` (findByUserId, create, update), `workouts.repository.ts` (CRUD + findByAuthor/findByClub + hasUpcomingEvents). Расширен `events.repository.ts` методом `updateTrainerFields()`. (4) **API роуты:** `GET/POST/PATCH /api/trainer/profile` (проверка isTrainerInAnyClub), `GET/POST/PATCH/DELETE /api/workouts` (проверка авторства, клубного членства, 409 при удалении привязанной тренировки). `PATCH /api/events/:id` для назначения тренировки/тренера (trainerId — только leader). Хелпер `trainer-role.ts` с isTrainerInAnyClub/isTrainerOrLeaderInClub/isLeaderInClub. (5) **Тесты:** 33 новых теста (trainer: 11, workouts: 16, events-patch: 6), итого 131 тест. (6) **Mobile модели и сервисы:** `TrainerProfile`, `Workout` модели с fromJson; `TrainerService`, `WorkoutsService` зарегистрированы в ServiceLocator. Расширена модель события полями workoutId/trainerId/workoutName/trainerName. (7) **Mobile UI:** 4 экрана — TrainerProfileScreen (просмотр профиля, chips специализаций, сертификаты), TrainerEditProfileScreen (форма с FilterChips, динамический список сертификатов), WorkoutsListScreen (табы Мои/Клубные, swipe-to-delete), WorkoutFormScreen (create/edit с dropdowns). (8) **Навигация:** 5 GoRouter routes (/trainer/:userId, /trainer/edit, /workouts, /workouts/create, /workouts/:id/edit). (9) **i18n:** ~50 ключей в оба ARB файла (профиль тренера, специализации, тренировки, типы, сложность, метрики, интеграция с событиями). Flutter analyze — 0 errors, backend tsc — clean. Подробности — docs/changes/2026-02-16-trainer-implementation.md.
- **Territory Card Redesign — фикс багов и доработка (2026-02-16):** Исправлены 8 багов и недореализованных секций из ревью спеки. (1) **[Critical] myClubProgress всегда null:** Backend `territories.config.ts` — добавлена функция `resolveMyClubProgress(leaderboard, userClubIds)`, split `materialize` на `materializeLight` (для карты, без leaderboard/myClubProgress) и `materializeFull` (для деталей). `GET /api/territories/:id` теперь async — резолвит клубы пользователя через `clubMembersRepo.findActiveClubsByUser()` и заполняет myClubProgress. CTA «БЕЖАТЬ ЗА ЗОНУ» теперь достижима для пользователей в клубах из leaderboard. (2) **[Bug] seasonEndsAt null-guard:** `territory_league_models.dart` — fallback на 1-е число следующего месяца при null. (3) **[Minor] LinearProgressIndicator clamp:** добавлен `.clamp(0.0, 1.0)`. (4) **[Missing] Lazy-load leagueInfo:** `MapService.getTerritoryDetails(id)` → `GET /api/territories/:id`. `TerritoryBottomSheet` теперь lazy-загружает полные данные при открытии; `getTerritoriesForCity` возвращает лёгкие DTO (без leaderboard). (5) **[Missing] Loading/Error/Retry:** добавлены состояния загрузки (CircularProgressIndicator), ошибки (иконка + текст + Retry). (6) **[Missing] CONTESTED badge:** в header — badge с кол-вом клубов при >=2 в leaderboard, оранжевый фон. (7) **[Minor] Leaderboard:** ограничение до top-10, ellipsis + строка своего клуба при position>10, hardcoded "km" заменён на i18n `leaderKm`. (8) **[Missing] Иконка захвата:** в `map_screen.dart` — PlacemarkMapObject с инициалами клуба в центроиде полигона при clubId != null.
- **Trainer 2.0 спецификация — доработка по ревью (2026-02-16):** Переработана `docs/trainer-implementation-spec.md` по результатам ревью (14 недочётов). Основные изменения: (1) TrainerProfile: убраны `rating`, `isVerified`, `socialLinks` из MVP → Future; добавлены правила доступа (редактирование только при роли trainer/leader, при понижении — read-only). (2) Workouts: убраны `isPublic` и `structure` из MVP → Future; MVP = простые шаблоны с текстовым description; добавлены visibility rules (clubId → члены клуба, null → только автор). (3) Event integration: уточнена семантика `trainerId` = ведущий тренер (не организатор); `actualDistance` → Future. (4) Новая секция API Endpoints: полный перечень с HTTP-методами, request/response примерами, правами доступа. (5) Новая секция Права доступа: матрица прав для всех действий, SQL-определение «является тренером». (6) Новая секция Ограничения MVP: явный список MVP vs Future. (7) Новая секция i18n ключи: ~50 ключей для обоих ARB файлов. (8) Новая секция Zod-схемы: примеры схем для TrainerProfile, Workout, Event extension DTO. (9) Roadmap переписан под MVP-скоуп (3 этапа вместо 4). (10) Конструктор тренировок (structure JSON) перенесён в секцию Future.
- **Territory Card Redesign — реализация UI + мок-данные (2026-02-16):** Реализованы секции 6.1–6.4, 6.6 спеки `docs/changes/2026-02-14-territory-card-redesign.md`. (1) Backend: добавлены типы `ZoneTier`, `LeaderboardEntryDto`, `ClubProgressDto` в `territory.dto.ts`; расширен `TerritoryViewDto` полями `tier`, `paceThreshold`, `pointMultiplier`, `zoneBounty`, `seasonEndsAt`, `leaderboard`, `myClubProgress`. В `territories.config.ts` добавлены: мок-активность 18 районов, `computeTier()` по формуле из спеки, `generateMockLeaderboard()`, `computeSeasonEndsAt()`, `TIER_CONFIG`. Цвет полигона теперь = цвет тира. (2) Mobile модели: создан `territory_league_models.dart` (`ZoneTier`, `LeaderboardEntry`, `ClubProgress`, `TerritoryLeagueInfo`); `TerritoryMapModel` расширен полем `leagueInfo`. (3) Mobile UI: создан `tier_colors.dart` (`TierColors` с градиентами); `TerritoryBottomSheet` полностью переработан (StatefulWidget, градиентный хедер, rules grid, battle progress, action bar, 4 состояния); создан `LeaderboardSheet` (топ-10, подсветка своего клуба). (4) Карта: fill opacity 0.3, stroke opacity 0.8. (5) i18n: 25 новых ключей в оба ARB файла. Секция 6.5 (DB, GPS intersection, cron) отложена — используются мок-данные. Backend: 98 тестов, mobile: 21 тест, flutter analyze — 0 errors (5 info pre-existing).
- **Спека «Territory Card Redesign» — ревью и фикс расхождений (2026-02-16):** Верификация спеки `docs/changes/2026-02-14-territory-card-redesign.md` против реального кода. Исправлено 3 расхождения: (1) ID районов в мок-данных (раздел 6.2) заменены на реальные из `spb-districts.data.ts` (префикс `spb-`, транслитерация `-skiy`/`-nyy`). (2) В раздел 6.1 добавлено пропущенное поле `capturedByUserId` в перечислении текущих полей DTO. (3) В раздел 6.4 добавлено описание архитектуры мобильных моделей (`TerritoryModel` vs `TerritoryMapModel`) и уточнено, что новые поля лиги добавляются в `TerritoryMapModel`, а `TerritoryLeagueInfo` — lazy-load через `GET /api/territories/:id`.
- **Спека «Territory Card Redesign» — финализация (2026-02-16):** Документ `docs/changes/2026-02-14-territory-card-redesign.md` переработан из черновика в готовую к реализации спецификацию. Добавлено: (1) Формализованная механика динамических тиров (Green/Blue/Red/Black) на основе суммарного объёма и среднего темпа в районе, с конкретными порогами и формулой. (2) Формула очков: base (1 км = 1 балл) × zone bounty (x1.5) × pace multiplier (x1.0–1.5, мягкий бонус). (3) Месячный цикл: сброс, пересчёт тиров, архивация. (4) Edge cases (0 активности, один клуб, пересечение зон, бегун без клуба, ничья, расформирование). (5) UI-состояния: без клуба (CTA), начало сезона, loading, error. (6) Лидерборд (топ-10 + позиция своего клуба). (7) API-контракт (расширение TerritoryViewDto: tier, leaderboard, myClubProgress, seasonEndsAt). (8) Мок-стратегия: фейковая активность по 18 реальным районам СПб вместо hashCode. (9) Бэкенд TODO-список. (10) i18n ключи.
- **Правило обновления docs/features (2026-02-15):** В .cursorrules добавлен пункт 4 в «ПРАВИЛА ДОКУМЕНТАЦИИ»: при изменении функционала вкладок обновлять docs/features/*.md, чтобы документы всегда соответствовали действительности.
- **Документация функционала вкладок (2026-02-15):** Создана директория `docs/features/` с отдельными документами по каждой вкладке приложения: [map-tab.md](features/map-tab.md), [run-tab.md](features/run-tab.md), [messages-tab.md](features/messages-tab.md), [events-tab.md](features/events-tab.md), [profile-tab.md](features/profile-tab.md). Индекс — [docs/features/README.md](features/README.md). Каждый документ описывает экраны, действия, API, навигацию и связанные файлы.
- **Firebase App Distribution: GOOGLE_APPLICATION_CREDENTIALS (2026-02-14):** deploy-mobile.ps1 и deploy-mobile.sh теперь предпочитают service account (GOOGLE_APPLICATION_CREDENTIALS) вместо FIREBASE_TOKEN — убирает deprecation warning. Fallback на FIREBASE_TOKEN сохранён. docs/build-and-share.md обновлён.
- **Feedback 2026-02-14 — реализация:** (1) Профиль: Геолокация, Видимость профиля, Выйти из аккаунта, Удалить аккаунт перенесены в «Редактирование профиля». ProfileScreen упрощён; EditProfileScreen принимает ProfileModel и содержит блок настроек; settings_section.dart удалён. (2) Backend: добавлена поддержка `onlyOpen=true` в GET /api/events — при onlyOpen применяется `status = 'open'`. Подробности — docs/changes/2026-02-14-profile-settings-and-onlyopen.md.
- **Feedback 2026-02-14 — перепроверка (2026-02-14):** Код проверен по чеклисту (профиль + onlyOpen). Автопроверки: backend `npx jest --runInBand` — 98 passed (в среде агента `npm test` падал с `spawn EPERM`); mobile `flutter analyze` — 0 issues; `flutter test` — 21 passed. Ручная проверка UI-фильтров событий на устройстве/эмуляторе не выполнялась.
- **Deploy mobile: App Distribution release notes (2026-02-14):** `scripts/deploy-mobile.ps1` теперь ограничивает длину release notes и не валит деплой, если APK успешно загружен, но обновление release notes упало. Подробности — docs/changes/2026-02-14-deploy-mobile-release-notes.md.
- **Захват территорий и мобильные фиксы (2026-02-14):** (1) Backend: реализован эндпоинт `POST /api/territories/:id/capture` с проверкой ADR-0007 (только активные члены клуба); добавлены DTO и Zod-валидация для захвата. (2) Mobile: исправлены утечки `FutureBuilder` в `CityPickerDialog` и `MapScreen` (Clubs BottomSheet) — теперь используются `StatefulWidget` с кэшированием `Future`. (3) Mobile: в `ChatWebSocketService` добавлено автоматическое переподключение и `statusStream` для управления polling-fallback в UI. (4) Тесты: добавлено 6 тестов для маршрутов территорий (backend). Подробности — docs/changes/2026-02-14-territory-capture-and-mobile-fixes.md.
- **Аудит модуля «Клубы» — реализация (2026-02-14):** Выполнены рекомендации из docs/tasks/gemini-clubs-audit-report.md. (1) Data Integrity: territories.config.ts — club-1/club-2 заменены на null, все территории FREE. (2) Backend: создание клуба всегда PENDING; авто-активация при approveMembership при 2+ участниках; передача лидерства с демоцией старого лидера (updateRoleWithLeaderTransfer). (3) Метрики: GET /api/clubs/:id — territoriesCount, cityRank. (4) Mobile: UX-подсказка при PENDING-клубе (clubActivationHint). (5) ADR-0007: захват только active-клубами; docs/changes/mercenaries-requirement.md. Подробности — docs/changes/2026-02-14-clubs-audit-implementation.md.
- **Территории: полигоны вместо кругов (2026-02-13):** Выполнены рекомендации из docs/tasks/gemini-map-zones-analysis-report.md (Quick Win). Backend: TerritoryViewDto.geometry (опционально), generateSquareGeometry в territories.config.ts — квадраты 1000×1000 м вокруг центра. Mobile: TerritoryMapModel.geometry, MapScreen — PolygonMapObject при наличии geometry, иначе CircleMapObject. Тесты: backend 87, mobile 21. Подробности — docs/changes/2026-02-13-territory-zones-polygons.md.
- **Brainstorm: вкладка Профиль MVP — реализация (2026-02-13):** Выполнены рекомендации из docs/changes/2026-02-13-profile-brainstorm.md. (1) Видимость профиля: backend — миграция 017_users_profile_visible, поле profile_visible в users; GET/PATCH /api/users/me/profile возвращают/принимают profileVisible; mobile — Switch в настройках вызывает UsersService.updateProfile(profileVisible). (2) Удаление аккаунта: подключён DELETE /api/users/me; UsersService.deleteAccount(); диалог подтверждения (deleteAccountTitle, deleteAccountConfirm, deleteAccountConfirmButton); после успеха — signOut и переход на /login. (3) Геолокация: тап на ListTile геолокации открывает настройки приложения через Geolocator.openAppSettings(). PersonalInfo/Activity не убирались. Уровень и видимость статистики отложены. Подробности — docs/changes/2026-02-13-profile-mvp-implementation.md.
- **Messages MVP — вариант A + заглушки (2026-02-13):** Принято решение: оставить 3 таба (Клуб, Личные, Тренер) с заглушками. Обновлены тексты заглушек: «Личные чаты — в разработке», «Сообщения тренера — в разработке» (personalChatsEmpty, coachMessagesEmpty в app_*.arb). Z7 (стабилизация ввода) уже реализован в ClubMessagesTab. Подробности — docs/changes/2026-02-13-messages-mvp-stubs.md.
- **Brainstorm: вкладка Сообщения MVP (2026-02-13):** Результаты brainstorm оформлены в docs/changes/2026-02-13-messages-brainstorm.md. Рекомендации: MVP = стабильный клубный чат (Z7 стабилизация ввода) + заглушки Личные/Тренер; подчаты, личные чаты, тренерские чаты — post-MVP.
- **Chat WebSocket (2026-02-13):** Реализован real-time чат через WebSocket. Mobile: новый `ChatWebSocketService` — подключение к `/ws`, подписка на `club:clubId` или `club:clubId:channelId`. ClubMessagesTab использует WebSocket как основной источник новых сообщений; при сбое — fallback на polling. Подробности — docs/changes/2026-02-13-chat-websocket.md.
- **Code review + рекомендации (2026-02-13):** (1) ClubMessagesTab: при `channels.length == 0` добавлен вызов `_openChannelChat(clubId, null)`. (2) Jest: добавлен `jest.setup.ts` с `afterAll(closeDbPool)`. (3) isValidUuid вынесен в `shared/validation.ts`, используется в messages.routes и organizer-display. Backend-тесты: 82 passed.
- **Z8 Swipe-to-run: реализация (2026-02-13):** Реализованы принятые решения: Backend — окно check-in 30 мин до — 1 ч после; фильтр participantOnly в GET /api/events. Mobile — карточка SwipeToRunCard (свайп = check-in + старт пробежки), фильтр «Участвую». Проверка 1 км отложена. Backend 82 теста, mobile 19 тестов. Подробности — docs/changes/2026-02-13-events-swipe-to-run-implementation.md.
- **Решения по check-in событий Z8 (2026-02-13):** Приняты 7 решений: Swipe-to-run, Check-in+Run, окно 30 мин до — 1 ч после, проверка 1 км, радиус 500 м, EventDetailsScreen + фильтр «Участвую», вне условий — disabled с пояснением. Оформлено в docs/changes/2026-02-13-events-checkin-decisions.md и infra/README.md.
- **Brainstorm: вкладка События MVP (2026-02-13):** Результаты brainstorm Gemini оформлены в infra/README.md (секция «События»): контекст из product_spec, идея контекстного check-in, три варианта UX (Swipe-to-run, Push, QR), рекомендация Swipe-to-run для MVP.
- **Док: упрощение infra/README.md (2026-02-12):** Выполненные задачи, багфиксы и подробные аудиты убраны из `infra/README.md`; файл теперь содержит только актуальный TODO по инфраструктуре/фичам и ссылки на `docs/progress.md` и `docs/changes/*` как основной источник правды.
- **Feedback (2026-02-12):** В `infra/README.md` зафиксирован новый продуктовый/UX‑фидбек по вкладке «Пробежка» (переименовать кнопку в «Старт», добавить паузу), клубам (вход по запросу, несколько подчатов), сообщениям (стабилизировать поведение ввода после отправки), событиям (убрать отдельную кнопку «Отметиться») и роли тренеров (нужна отдельная проработка функционала).
- **Deploy verification (2026-02-12):** Выполнен полный `npm run deploy` (backend + mobile). GitHub Actions CI run `21961188876` завершился успешно. Backend обновлён на сервере через `~/runterra/backend/update.sh` и применил миграцию `015_club_channels`. Mobile: `flutter test` прошёл, debug APK собран и отправлен в Firebase App Distribution (версия `0.1.0+97`).
- **Стрелка направления на карте трекинга (2026-02-10):** В RunRouteMap синий кружок текущей позиции заменён на навигационную стрелку направления движения. Стрелка рисуется программно (dart:ui Canvas) — синий шеврон в белом круге, поворачивается по `heading` из GPS (geolocator `Position.heading`). Используется `PlacemarkMapObject` с `direction` и `rotationType: RotationType.rotate`. При отсутствии heading (heading < 0) — fallback на старый синий кружок. Подробности — docs/changes/run-history.md.
- **Журнал тренировок (история пробежек, 2026-02-10):** Реализован полный цикл просмотра истории пробежек. Backend: добавлены 3 GET-эндпоинта в runs.routes.ts — `GET /api/runs` (список пробежек с пагинацией), `GET /api/runs/stats` (статистика пользователя), `GET /api/runs/:id` (детали пробежки с GPS-треком и проверкой владельца). Добавлены DTO: RunHistoryItemDto, RunDetailDto, UserRunStatsDto. Mobile: создано 5 новых файлов — RunTrackingScreen (вынесен из RunScreen), RunHistoryScreen (журнал тренировок со статистикой и списком), RunDetailScreen (детали пробежки с картой маршрута), RunDetailMap (статическая карта с полилинией и маркерами старт/финиш). RunScreen переписан как роутер: при наличии активной сессии → трекинг, иначе → журнал. Модели: RunHistoryItem, RunStats, RunDetailModel (extends RunModel + gpsPoints), GpsPointModel. RunService дополнен 3 методами: getRunHistory, getRunStats, getRunDetail. Навигация: добавлен роут `/run/detail/:id`. i18n: 13 новых ключей (en/ru). Backend: 80 тестов. Mobile: flutter analyze — 4 info (pre-existing), flutter test — 19 тестов. Подробности — docs/changes/run-history.md.
- **Feedback 2026-02-09 — 8 задач UX (события, карта, клубы):** Реализованы 8 задач из feedback (infra/README.md Feedback 2026-02-09):
  1. **Город: spb → «Санкт-Петербург»:** В create_event_screen убран fallback на cityId, вместо него — l10n ключ `eventCreateSelectCity`.
  2. **Основатель клуба:** Backend GET /api/clubs/:id теперь возвращает `creatorId` и `creatorName` (resolve через usersRepo). Mobile: ClubModel расширена полями creatorId/creatorName, на ClubDetailsScreen добавлен ListTile с иконкой звезды и именем основателя.
  3. **Список клубов по городу:** Создан ClubsListScreen (новый файл), добавлен GoRouter route `/clubs?cityId=`. На MyClubsScreen добавлена кнопка «Все клубы города».
  4. **organizerId/organizerType — автоопределение:** В CreateEventScreen убран TextFormField для organizerId. Автозаполнение: если есть текущий клуб → organizerId=clubId, type=club; иначе → из профиля, type=trainer. Dropdown organizerType виден только если пользователь в клубе.
  5. **Маркеры событий на карте:** В MapScreen добавлены программные маркеры событий (dart:ui Canvas). По тапу — bottom sheet с деталями и кнопкой «Подробнее» → /event/:id.
  6. **События клуба на экране деталей:** На ClubDetailsScreen добавлена секция «Ближайшие события» с EventCard (до 3 шт.) и кнопкой «Все события клуба».
  7. **Выбор точки старта на карте:** Создан LocationPickerScreen (новый файл) — полноэкранная Yandex карта с пином по центру. Интегрирован в CreateEventScreen: кнопка «Выбрать на карте» вместо ручного ввода lat/lon.
  8. **Выход лидера из клуба:** Backend: валидация leader в POST /api/clubs/:id/leave (блокировка если >1 участник); новый DELETE /api/clubs/:id (роспуск). Mobile: диалоги для лидера (передать/распустить/отмена), новый TransferLeadershipScreen, метод disbandClub в ClubsService, метод delete в ApiClient.
  Backend: 80 тестов пройдено. Mobile: flutter analyze — 3 info (все pre-existing deprecated_member_use), flutter test — 19 тестов пройдено. i18n: 21 новый ключ в оба ARB файла. Подробности — docs/changes/feedback-2026-02-09.md.
- **Feedback UX (2026-02-09):** Реализованы 6 пунктов из feedback (2026-02-08, UX и данные):
  1. **Клубы в сообщениях:** исправлен GET /api/messages/clubs — теперь используется `findActiveClubsByUser` вместо `findActiveByUser`, возвращаются реальные названия и описания клубов.
  2. **Список участников клуба:** добавлены GET /api/clubs/:id/members (список с userId, displayName, role, joinedAt) и PATCH /api/clubs/:id/members/:userId/role (смена роли, только leader). На mobile: секция «Участники» на ClubDetailsScreen с ролями; лидер может менять роли через bottom sheet.
  3. **Шапка профиля:** убраны клуб/роль/меркатель из ProfileHeaderSection — остались только ФИО + город.
  4. **Личные данные:** ProfilePersonalInfoSection преобразован в StatefulWidget, свёрнут по умолчанию с expand/collapse по тапу на заголовок.
  5. **Мини-карта события:** placeholder заменён на YandexMap с CircleMapObject в точке старта (read-only, zoom 15). По тапу — переход на вкладку Карта с центрированием (`/map?lat=..&lon=..`). MapScreen принимает focusLatitude/focusLongitude.
  6. **Документация:** обновлены infra/README.md (все 6 feedback items + PATCH клуба + myRole помечены [x]), progress.md, changes/*.md.
  Backend: 80 тестов пройдено. Mobile: flutter analyze — 5 info (все pre-existing), flutter test — 19 тестов пройдено.
- **Сообщения: выбор клуба перед чатом (2026-02-08):** Во вкладке «Сообщения → Клуб» первым шагом отображается список клубов пользователя (из GET /api/messages/clubs); чат открывается только после выбора клуба. Добавлена кнопка «Назад» для возврата к списку. Поддержан deep-link `/messages?tab=club&clubId=...` и переход с экрана клуба «Чат клуба» на чат именно этого клуба. Добавлены l10n: messagesBackToClubs, messagesSelectClub. Подробности — docs/changes/messages.md.
- **Профиль: экран «Мои клубы» (2026-02-08):** Реализован отдельный сценарий просмотра всех клубов пользователя. Backend: добавлен `GET /api/clubs/my` и репозиторный метод `findActiveClubsByUser()` с возвратом `id/name/cityId/cityName/status/role/joinedAt`. Добавлены API-тесты на успешный сценарий и `401` для отсутствующего пользователя. Mobile: добавлены `MyClubModel`, `ClubsService.getMyClubs()`, кнопка «Клубы» в профиле, экран `/profile/clubs` со списком клубов, состояниями loading/empty/error+retry и переходом в детали клуба. Добавлены локализации ru/en и тест модели. Подробности — docs/changes/clubs.md.
- **Организатор события — отображаемое имя (2026-02-08):** Во вкладке «Событие» вместо сырого ID организатора теперь показывается имя создателя события (название клуба или имя тренера). Backend: в DTO событий добавлено опциональное поле `organizerDisplayName`; для списков (GET /api/events, GET /api/map/data) используется батч-резолв `getOrganizerDisplayNamesBatch` (два запроса к БД вместо N); для деталей — `getOrganizerDisplayName`. В `ClubsRepository` добавлен `findByIds`. Mobile: пустая/пробельная строка `organizerDisplayName` обрабатывается как отсутствие имени (fallback на ID). Добавлены тесты API на наличие `organizerDisplayName` в ответах. Подробности — docs/changes/events.md.
- **Редактирование профиля клуба для лидера (2026-02-08):** Реализована функциональность редактирования клуба согласно product_spec.md §5.3. Backend: добавлен PATCH /api/clubs/:id с проверкой роли leader (только лидер может редактировать); создан UpdateClubDto и UpdateClubSchema с валидацией name (3-50 символов) и description (до 500 символов); обновлён GET /api/clubs/:id для возврата поля userRole ('member' | 'trainer' | 'leader' | null). Mobile: добавлено поле userRole в ClubModel; создан EditClubScreen для редактирования названия и описания с валидацией на клиенте; на ClubDetailsScreen добавлена кнопка "Редактировать клуб" (видна только лидерам); добавлена локализация en/ru. Подробности — docs/changes/clubs.md.
- **Feedback (2026-02-08) — клубы и события (исправлено):** (1) Клубы: добавлено поле role в таблицу club_members (миграция 013_club_members_role.sql) с ролями member/trainer/leader; создатель клуба теперь получает роль leader вместо member при создании; добавлен метод countActiveMembers() в ClubMembersRepository; GET /api/clubs/:id возвращает реальное количество участников из БД вместо заглушки 0; обновлены моки в тестах. (2) События: исправлен фильтр "Только открытые" — добавлено условие (end_date_time IS NULL OR end_date_time > NOW()) в фильтр по умолчанию EventsRepository.findAll(), прошедшие события (где end_date_time < NOW) теперь не возвращаются при фильтре "открытые". Все тесты проходят (77 passed). Подробности — infra/README.md.
### 2026-02-09
- **Багфикс: карта не приближается к событию при переходе с мини-карты (v2):** Корневая причина — `_handleCameraPositionChanged` (обработчик границ города) перехватывал промежуточные кадры анимации `_flyToFocusPoint` и clamp'ил координаты к bounds города (начальная позиция камеры далеко за пределами SPb). Исправление: (1) добавлен флаг `_isAnimatingToFocus`, который блокирует `_handleCameraPositionChanged` во время анимации к фокусной точке; (2) `_flyToFocusPoint` переведён с `void` на `Future<void>` с `await moveCamera`; (3) добавлена задержка 100ms перед moveCamera для готовности native view; (4) `_onMapCreated` теперь await'ит `_flyToFocusPoint`. Предыдущий фикс (didUpdateWidget + _hasFocusPoint) решал другую проблему (ShellRoute кэширование) и остаётся.
- **Багфикс: карта не приближается к событию при переходе с мини-карты:** В `MapScreen` добавлен `didUpdateWidget`, который отслеживает изменения `focusLatitude`/`focusLongitude` и вызывает `_flyToFocusPoint()` — перемещение камеры к нужным координатам. Ранее ShellRoute переиспользовал виджет и `initState` не вызывался повторно. Логика перемещения вынесена в отдельный метод `_flyToFocusPoint()`, используется и в `_onMapCreated`, и в `didUpdateWidget`.
- **Багфикс: организатор события показывает UUID вместо имени:** В `organizer-display.ts` для `organizerType = 'trainer'` добавлен приоритет `firstName + lastName` над `name`. Ранее `user.name` часто было пустым или содержало дефолт, тогда как реальное имя хранится в `firstName`/`lastName`. Исправлено в обеих функциях: `getOrganizerDisplayName` (single) и `getOrganizerDisplayNamesBatch` (batch).
- **Багфикс: `string_to_uuid` в batch resolve organizer names (из логов):** В таблице `events` обнаружены исторические записи с невалидными `organizer_id` (`'1'`, `'demo-club-1'`). `getOrganizerDisplayNamesBatch` передавал их в `findByIds`, PostgreSQL не мог привести к UUID → warn в логах (25+ раз за 08-09.02). Добавлена UUID-валидация перед запросами к БД в обеих функциях. Тестовые моки обновлены с `'club-1'` на валидный UUID.

### 2026-02-08- **Исправление архитектурных недостатков клубов (критично, P0):** Исправлены критические проблемы целостности данных и валидации clubId. (1) Добавлен Foreign Key между `club_members.club_id` и `clubs.id`: создана миграция `012_clubs_fk.sql`, которая конвертирует `club_members.club_id` из `VARCHAR(128)` в `UUID`, удаляет orphaned records и добавляет FK с каскадным удалением; убран костыль `id::text` из `ClubsRepository.findById()`. (2) Унифицирован формат clubId: валидация `isValidClubId()` изменена с slug-формата на строгий UUID; обновлены комментарии в `backend/src/shared/clubId.ts`. (3) Добавлена проверка существования клуба в `POST /api/clubs/:id/join` и `POST /api/clubs/:id/leave`: теперь перед созданием/обновлением membership вызывается `clubsRepo.findById()`, и возвращается 404 с кодом `not_found`, если клуб не найден. (4) Обновлены тесты: в `api.test.ts` slug-based ID заменены на валидные UUID-константы; добавлены моки `mockClubsRepository.findById()`. Все тесты проходят (77 passed). Недостатки задокументированы в `infra/README.md`.
## стория изменений

### 2026-02-07
- **Проверка логов runterra (errors-runbook):** Найдена новая ошибка уровня error, которой нет в разделе «Исправлено» runbook: Error fetching club (22P02, string_to_uuid) при GET /api/clubs/new-club-id (в логах путь как /new-club-id, параметр $1). Ошибка добавлена в docs/errors-runbook.md в «Известные открытые». По контексту связана с недавними изменениями вокруг строкового clubId: часть системы уже использует string IDs (club-1, club-2), но в этом маршруте остаётся ожидание UUID/валидация отсутствует.
### 2026-02-06
- **Mobile Messages — stale async guard:** `ClubMessagesTab` now ignores delayed polling/history responses when active `clubId` changes before the response arrives.
- **Mobile Messages — вкладка «Клуб» как реальный чат (feedback):** справлена проблема «нет чата/нет поля ввода» во вкладке `Сообщения -> Клуб`. `ClubMessagesTab` переведён со списка клубных чатов на экран текущего клубного чата: загрузка истории через `MessagesService.getClubChatMessages(clubId)` и отправка через `MessagesService.sendClubMessage(clubId, text)`. `clubId` берётся из `CurrentClubService.currentClubId`, а при его отсутствии используется fallback на первый доступный клуб из `getClubChats()` с сохранением в `CurrentClubService`. Внутри тела вкладки убрана надпись с названием клуба; оставлены только сообщения и composer. Добавлены состояния loading/error/retry и empty-state при отсутствии клуба. Подробности — [changes/messages.md](changes/messages.md).
- **Mobile Messages — стабилизация вкладки «Клуб» (регрессии после первичного фикса):** Закрыты потенциальные риски: (1) корректно определяются «мои» сообщения по backend `users.id` из профиля (с fallback на Firebase uid), (2) stale `currentClubId` теперь валидируется по фактическому membership из `GET /api/messages/clubs`, (3) fallback-клуб выбирается по `primaryClubId` или по активности, а не по «первому в списке», (4) добавлена подгрузка старых сообщений при скролле вверх, (5) добавлено периодическое обновление новых сообщений (polling), (6) для ошибок загрузки истории используется `messagesLoadError`, (7) автоскролл сделан плавным и не перехватывает управление, если пользователь читает историю. Подробности — [changes/messages.md](changes/messages.md).
- **Расширение экрана клуба (ClubDetailsScreen, infra/README.md):** На экране деталей клуба добавлены: (1) город клуба — отображается под названием (cityName из backend или cityId); (2) метрики MVP — три карточки: участники, территории, рейтинг в городе (значения из API или заглушка «—»); (3) кнопка «Чат клуба» — переход на экран Сообщения с открытой вкладкой «Клуб» (`/messages?tab=club`). Backend: GET `/api/clubs/:id` дополнен полями `cityName` (из конфига городов), `membersCount`, `territoriesCount`, `cityRank` (плейсхолдеры 0). Mobile: ClubModel расширен полями cityId, cityName, membersCount, territoriesCount, cityRank; MessagesScreen принимает `initialTabIndex` для открытия с нужной вкладки; маршрут `/messages?tab=club` передаёт initialTabIndex: 1. i18n: clubChatButton, clubMembersLabel, clubTerritoriesLabel, clubCityRankLabel, clubMetricPlaceholder (EN/RU). Подробности — [changes/clubs.md](changes/clubs.md).

- **Проверка логов runterra (errors-runbook):** Просмотрены journalctl и error-2026-02-06.log. Новых ошибок не найдено: в логах за 06.02 только warn (POST/GET "/" → 404 — ожидаемо). Ошибки уровня error в journalctl — исторические от 03.02 (global messages / string_to_uuid), уже в разделе «справлено» runbook. В progress и runbook добавлено примечание про ожидаемые 404 на "/".

- **Аудит: bulk-insert GPS точек (infra/README.md, 2026-02-04):** справлен bulk-insert GPS точек в `RunsRepository.saveGpsPoints(runId, points)`: плейсхолдеры теперь строятся с правильным смещением (`offset = index * 5` для 5 столбцов `run_gps_points`), параметры передаются в порядке `[runId, longitude, latitude, timestamp, point_order]` для каждой точки. Добавлен юнит-тест в `runs.repository.test.ts`, который вызывает `RunsRepository.create()` с двумя GPS‑точками и проверяет, что во второй DB‑запрос (`INSERT INTO run_gps_points`) передаётся корректный SQL и 10 параметров (2 точки × 5 полей) с ожидаемыми значениями. Пункт аудита в infra/README.md помечен как выполненный.
 
- **Аудит: clubId — единый строковый формат во всех слоях (infra/README.md, 2026-02-04):** Принят единый формат идентификатора клуба как строковый (opaque string) во всех слоях. В БД добавлена миграция `009_messages_channel_id_varchar.sql`, которая меняет тип `messages.channel_id` с `UUID` на `VARCHAR(128)`, чтобы он соответствовал `club_members.club_id` и строковым `clubId`/`cityId` в доменной модели. В WebSocket-сервере (`chatWs.ts`) регулярное выражение `VALID_CHANNEL_RE` ослаблено до формата `club:{clubId}` с допустимыми символами `[A-Za-z0-9_-]{1,128}`, чтобы поддерживать строковые идентификаторы (например, `club-1`, `spb-runner-club`) вместо строгого UUID. Таким образом, clubId/club_id/channel_id согласованы как строки в DB, API (`clubId` в DTO и маршрутах), WS (каналы `club:<id>`) и mobile (String). Подробности — [changes/messages.md](changes/messages.md).

- **Аудит: авто-создание пользователя по валидному токену (infra/README.md, 2026-02-04):** Задача покрыта существующей реализацией `GET /api/users/me/profile`: при первом запросе с валидным `Authorization: Bearer <token>` backend через `req.authUser.uid` ищет пользователя в `UsersRepository.findByFirebaseUid`, и если запись не найдена, автоматически создаёт нового пользователя на основе auth-данных (`displayName`, `email`, `photoURL`) и сохраняет его в БД перед формированием `ProfileDto`. Таким образом, успешная верификация токена (auth stub/Firebase в будущем) автоматически онбордит пользователя в систему, а остальные user-эндпоинты (`PATCH /api/users/me/profile`, `DELETE /api/users/me`) ожидают уже созданную запись и при её отсутствии возвращают 404 `user not found`. Подробности — [changes/users.md](changes/users.md).

- **Аудит: проверка членства для клубных чатов (HTTP + WS) (infra/README.md, 2026-02-04):** Для REST-эндпоинтов клубных чатов `GET /api/messages/clubs/:clubId` и `POST /api/messages/clubs/:clubId` добавлена проверка членства: после auth по `req.authUser.uid` backend находит пользователя через `UsersRepository.findByFirebaseUid`, затем проверяет активное членство в клубе через `ClubMembersRepository.findByClubAndUser(clubId, user.id)`; при отсутствии или неактивном статусе возвращается `403 forbidden` с кодом `forbidden` и сообщением `User is not a member of this club`. В WebSocket-сервере (`chatWs.ts`) функция `canSubscribe(uid, channelKey)` теперь, помимо проверки формата канала `club:{clubId}`, находит пользователя по `firebaseUid` и проверяет его активное членство в клубе; при отсутствии членства подписка отклоняется (клиент получает `{ type: 'error', message: 'Subscribe denied' }`). Таким образом, доступ к клубному чату (HTTP и WS) ограничен только участниками соответствующего клуба. Подробности — [changes/messages.md](changes/messages.md).

- **Аудит: унификация формата ошибок API (infra/README.md, 2026-02-04):** Оставшиеся эндпоинты, которые возвращали ошибки в виде `{ error: "..." }`, приведены к единому формату ADR-0002 `{ code, message, details? }`. В модуле событий (`events.routes.ts`) `GET /api/events`, `GET /api/events/:id`, `GET /api/events/:id/participants` и `POST /api/events` теперь при внутренних ошибках возвращают `500` с `{ code: "internal_error", message: "Internal server error" }`, а отсутствие события (`Event not found`) в `GET /api/events/:id` и `GET /api/events/:id/participants` отдаёт `404` с `{ code: "not_found", message: "Event not found", details: { eventId } }`. В модуле карты (`map.routes.ts`) `GET /api/map/data` при внутренних ошибках также возвращает `500` с `{ code: "internal_error", message: "Internal server error" }` вместо `{ error: "Internal server error" }`. Остальные роутеры уже использовали формат ADR-0002 ранее. Подробности — [changes/events.md](changes/events.md), [changes/maps.md](changes/maps.md), [docs/api-errors-and-validation.md](api-errors-and-validation.md).

- **Аудит: флоу событий на mobile (создание, join, check-in, фильтры) (infra/README.md, 2026-02-04):** На стороне mobile завершён минимальный флоу работы с событиями. Экран списка (`EventsScreen`) использует `EventsService.getEvents(cityId, dateFilter, clubId, onlyOpen, ...)`, фильтры даты, «Только открытые» и «Мой клуб» реально прокидываются на backend (через query-параметры), а FAB ведёт на `CreateEventScreen`, где `EventsService.createEvent()` создаёт событие и после успеха выполняется переход на `EventDetailsScreen` по `event.id`. Детальный экран (`EventDetailsScreen`) завершён в части участия и check-in: кнопки «Присоединиться» / «Вы участвуете» / «Отменить участие» вызывают `EventsService.joinEvent/leaveEvent` и обновляют состояние, а новая кнопка check-in для участников использует `LocationService.getCurrentPosition()` и `EventsService.checkInEvent(eventId, longitude, latitude)`; все ошибки обрабатываются через `ApiException` с кодами ADR-0002 и показываются через Snackbar с локализованными текстами `eventJoinError` / `eventLeaveError` / `eventCheckInError`. Методы `getEventById` и `getEventParticipants` в `EventsService` также приведены к единой обработке ошибок (через `ApiException`), устраняя падения при 404/500. Подробности — [changes/events.md](changes/events.md).

- **Аудит: убрать моки и подключить реальные данные для территорий/клубов/активностей (infra/README.md, 2026-02-04):** Для территорий и карты вместо абстрактных "Test Territory"/локальных mock‑массивов введён конфиг `modules/territories/territories.config.ts` с тремя реальными популярными беговыми локациями в Санкт‑Петербурге (`spb-primorsky-park-pobedy`, `spb-yalagin-island`, `spb-park-300`), а эндпоинты `GET /api/territories` и `GET /api/map/data` используют `getTerritoriesForCity(cityId, clubId?)` / `getTerritoryById(id)` и корректно обрабатывают 404. Для клубов `GET /api/clubs` больше не возвращает `Test Club`, а отдаёт статический список осмысленных клубов (`club-1` / `club-2`), связанных с этими территориями. Активности по‑прежнему остаются skeleton‑заглушками без БД, но базовый контракт и runtime‑валидация уже есть; данные для территорий и клубов стали реалистичными и согласованными с картой. Подробности — [changes/territories.md](changes/territories.md), [changes/maps.md](changes/maps.md), [changes/clubs.md](changes/clubs.md).

- **Аудит: mobile chat API для клубных/личных чатов (infra/README.md, 2026-02-04):** В `MessagesService` реализован HTTP‑клиент для клубных чатов поверх существующих backend‑эндпоинтов: `getClubChats()` (GET `/api/messages/clubs` → `ClubChatModel[]`), `getClubChatMessages(clubId, { limit, offset })` (GET `/api/messages/clubs/:clubId`) и `sendClubMessage(clubId, text)` (POST `/api/messages/clubs/:clubId`, возвращает `MessageModel`). Во всех трёх методах ошибки 4xx/5xx разбираются по ADR‑формату `{ code, message }` и пробрасываются в виде `ApiException`, чтобы UI мог показывать локализованные сообщения. Для личных чатов backend‑API ещё нет, поэтому `getPrivateChats()` и `getChatMessages(chatId)` возвращают пустые списки (stub), а `sendChatMessage` явно бросает `UnimplementedError('Personal chats are not implemented in MVP')`. Таб "Личные" по‑прежнему отображает `personalChatsEmpty`, что соответствует доменной спецификации (личные переписки не входят в MVP). Подробности — [changes/messages.md](changes/messages.md).

- **Аудит: транзакционная защита participant_limit для событий (infra/README.md, 2026-02-04):** В `EventsRepository.joinEvent` подтверждена и задокументирована текущая модель ограничения участников: проверка статуса события (`OPEN`/`FULL`) и `participantLimit` происходит перед созданием/обновлением записи в `event_participants`, после чего `updateParticipantCount()` пересчитывает `participant_count` и обновляет статус события (при достижении лимита → `FULL`, при освобождении мест — обратно в `OPEN`). Явных SQL‑транзакций и row‑level locking пока нет (skeleton‑этап, упрощённый DB‑stub), это отмечено как TODO для будущего этапа с реальной БД и нагрузкой; на текущем этапе этого достаточно для предотвращения оверсабскрайба в типичном однопоточном/низкоконкурентном окружении. Подробности — [changes/events.md](changes/events.md).

- **Аудит: исправить 500 на /api/messages при отсутствии auth (infra/README.md, 2026-02-04):** В `messages.routes.ts` вместо выбрасывания исключения при отсутствии `req.authUser.uid` (что приводило к общей 500 internal_error) добавлен helper `getAuthUidOrRespondUnauthorized(req, res)`, который возвращает 401 с ADR‑ошибкой `unauthorized` и причиной `missing_header` и завершает обработку запроса. Все маршруты `/api/messages/clubs` / `/api/messages/clubs/:clubId` (GET/POST) используют этот helper перед доступом к пользователю и проверкой членства в клубе, так что теперь при запросах без токена клиенты стабильно получают 401/403 вместо 500. Подробности — [changes/messages.md](changes/messages.md).
- **Аудит: Firebase Admin SDK и production baseUrl для mobile (infra/README.md, 2026-02-04):**
  - **Firebase Admin SDK:** В `FirebaseAuthProvider` интегрирован `firebase-admin`: при наличии `FIREBASE_PROJECT_ID`, `FIREBASE_CLIENT_EMAIL`, `FIREBASE_PRIVATE_KEY` backend инициализирует Admin SDK и использует `admin.auth().verifyIdToken()` для проверки токенов; в production отсутствие этих переменных приводит к падению старта сервера через `assertFirebaseAuthConfigured()`, в non-prod без конфигурации остаётся безопасная заглушка (derive uid из токена) для удобства разработки. Добавлена зависимость `firebase-admin` и обновлена документация (`changes/auth.md`, `firebase-setup.md`, `backend/README.md`).
  - **Production baseUrl для mobile:** В `ApiConfig.getBaseUrl()` фиксирован продакшн backend для мобильных/десктоп‑клиентов: при отсутствии `API_BASE_URL` и в release‑сборке по умолчанию используется `https://85.208.85.13:3000` (без localhost), в debug по‑прежнему `http://85.208.85.13:3000`, а для Flutter Web дефолт остаётся `https://localhost:3000` (ожидается reverse-proxy). Поведение задокументировано в `changes/api-client.md`.

### 2026-02-05
- **Tests: events/clubs/users + mobile models:** Added API tests for event leave and club leave, membership flags, repository tests for leaveEvent and birthDate mapping, and Flutter model tests for EventDetails/Club membership fields.
- **Fix: участие в событиях + создание события + расширение профиля:** Добавлены «Вы участвуете»/«Отменить участие» (backend `POST /api/events/:id/leave`, статус участия в деталях) и экран создания события на mobile (FAB → CreateEventScreen, `createEvent()` подключён к API). Профиль расширен личными данными (имя/фамилия/дата рождения/страна/пол), добавлена миграция `007_users_profile_fields.sql`, форма редактирования и секция отображения. Подробности — [changes/events.md](changes/events.md), [changes/users.md](changes/users.md).
- **Fix: пол/дата рождения/клубы/текст join:** Пол ограничен «мужской/женский» (миграция `008_users_gender_restrict.sql`, UI без other/unknown), дата рождения возвращается без UTC‑сдвига, join‑текст больше не содержит TODO, добавлен выход из клуба (`POST /api/clubs/:id/leave` + кнопка). Подробности — [changes/users.md](changes/users.md), [changes/clubs.md](changes/clubs.md), [changes/events.md](changes/events.md).

### 2026-02-04
- **Fix: фидбек теста (участники/клубы/профиль/сообщения):** Закрыты пункты из feedback секции infra/README. Backend: non-prod auth stub больше не возвращает фиксированный uid (uid из JWT или хэша токена), добавлен `GET /api/events/:id/participants`, профиль возвращает `club` по `primaryClubId`, добавлен `GET /api/messages/clubs` на основе `club_members`. Mobile: `ParticipantsList` загружает участников из API, профиль использует fallback по `primaryClubId`, `MessagesService.getClubChats()` подключён к backend. Подробности — [changes/events.md](changes/events.md), [changes/users.md](changes/users.md), [changes/messages.md](changes/messages.md), [changes/auth.md](changes/auth.md).
- **Fix: Повторный старт пробежки — `Exception: Run already started` (mobile):** справлена ошибка, при которой пользователь не мог начать новую пробежку после завершения предыдущей или при ошибке отправки. Причина: `RunService._currentSession` очищалась только при успешном `submitRun()` или явном `cancelRun()`. Решение: (1) добавлен метод `clearCompletedSession()` в `RunService`; (2) `startRun()` автоматически очищает completed-сессии; (3) `RunScreen.initState()` восстанавливает UI для running и completed сессий; (4) `_backToIdle()` вызывает `clearCompletedSession()`; (5) при исключении «Run already started» показывается диалог с выбором «Продолжить» или «Отменить и начать заново». Добавлены i18n ключи (EN/RU). Подробности — [changes/runs.md](changes/runs.md), [errors-runbook.md](errors-runbook.md).

- **Code review — Карта и трекинг (infra/README.md):** Проверена реализация трёх пунктов раздела «Карта и трекинг». Найдена и исправлена одна проблема: в `map_screen.dart` в методе `_showClubsBottomSheet()` при ошибке загрузки списка клубов кнопка показывала текст «Повторить» (`retry`), но фактически закрывала bottom sheet (`Navigator.pop`); исправлено на `cancel` («Закрыть»). Остальная реализация корректна: навигация FindClubAction/CreateClubAction, параметр showClubs в MapScreen, форма CreateClubScreen, ClubsService.createClub(), i18n ключи. Подробности — [changes/maps.md](changes/maps.md).
- **Карта и трекинг (infra/README.md):** Реализованы три пункта раздела «Карта и трекинг». (1) Кнопка «Найти клуб»: переход на карту с query `showClubs=true`; MapScreen при открытии с этим параметром показывает bottom sheet со списком клубов города (ClubsService.getClubs); тап по клубу — переход на экран клуба. (2) Кнопка «Создать клуб»: навигация CreateClubAction → `/club/create`; экран CreateClubScreen с формой (название, описание), город из CurrentCityService; ClubsService.createClub() — POST /api/clubs; при успехе переход на экран созданного клуба. (3) Убраны с карты фильтры: удалены панель фильтров (сегодня/неделя/мой клуб/только активные территории), кнопка фильтра в AppBar; данные карты запрашиваются только с cityId. Подробности — [changes/maps.md](changes/maps.md), [changes/clubs.md](changes/clubs.md).
- **Трекинг пробежки на карте — отображение маршрута в реальном времени во время бега (mobile):** В состоянии running экрана Run добавлена карта (Yandex MapKit) с полилинией маршрута из GPS-точек сессии. Виджет `RunRouteMap` принимает `gpsPoints`, рисует полилинию при двух и более точках; камера при первом появлении точки центрируется на ней (zoom 16), при появлении новых точек следует за последней позицией. Начальная позиция карты при отсутствии точек — центр текущего города (CurrentCityService) или СПб. Backend без изменений. Подробности — [changes/runs.md](changes/runs.md), [changes/maps.md](changes/maps.md).
- **Code review — Трекинг пробежки на карте:** Проверена реализация отображения маршрута в реальном времени. Найдена и исправлена одна проблема: в `run_screen.dart` в методе `_buildRunningContent()` для отображения GPS-статуса использовались одновременно `Icon(Icons.location_on)` и эмодзи `📍` в тексте (дублирование иконки). справлено: эмодзи удалён из текста, оставлена только Material-иконка. Остальная реализация корректна: виджет `RunRouteMap` правильно строит полилинию, следует за бегуном, корректно обрабатывает fallback на центр города. Подробности — [changes/runs.md](changes/runs.md).
- **Выбор города и События для примера:** (1) Выбор города: в GET `/api/users/me/profile` добавлено заполнение `cityName` из конфига городов (`findCityById(user.cityId)?.name`); в ProfileScreen блок «Город» отображает название города (cityName или fallback на cityId). (2) События для примера: добавлена миграция `005_seed_example_events.sql` — вставка 5 демо-событий для города spb (group_run, training, club_event), даты в будущем, координаты в границах СПб; после применения миграции список событий и карта показывают примеры. Подробности — [changes/users.md](changes/users.md), [changes/events.md](changes/events.md).
- **Code review — Выбор города и События для примера:** Проверена корректность реализации. Верифицировано: (1) Backend `users.routes.ts:87` — заполнение `cityName` из конфига корректно; (2) Миграция `005_seed_example_events.sql` — колонки соответствуют схеме events, значения типов/статусов валидны, координаты в границах SPB (lon 29.6–30.6, lat 59.7–60.1), UUID корректные, idempotent через `ON CONFLICT`; (3) Mobile `profile_screen.dart` — fallback логика отображения города корректна (`cityName ?? cityId ?? l10n.cityNotSelected`); (4) `ProfileModel.cityName` корректно парсится из JSON. Ошибок не обнаружено.
- **Code review — Завершение тренировки:** Проверена реализация экрана завершения пробежки. Найдена и исправлена одна проблема: в методе `_calcCalories()` неиспользуемый параметр `duration` удалён из сигнатуры (калории считаются только по дистанции ~65 ккал/км). Остальная реализация корректна: расчёты темпа, скорости, калорий; i18n ключи; layout карточек метрик. Подробности — [changes/runs.md](changes/runs.md).
- **Завершение тренировки — экран как в фитнес-приложении (mobile):** Экран результата пробежки переработан по образцу Strava/Nike Run Club. Карточки метрик: длительность, дистанция, темп (мин/км), средняя скорость (км/ч), калории (примерная оценка ~65 ккал/км), пульс (placeholder «—» под будущую Health Connect). Добавлены helper-методы `_formatPace`, `_calcAvgSpeedKmh`, `_calcCalories`, виджет `_buildMetricCard`. i18n: runDuration, runDistance, runPace, runPaceValue, runAvgSpeed, runAvgSpeedValue, runCalories, runCaloriesValue, runHeartRate, runHeartRateValue, runNoData. Backend без изменений. Подробности — [changes/runs.md](changes/runs.md).
- **Удаление чата города (backend + mobile):** Удалён городской чат. Backend: удалены эндпоинты GET/POST `/api/messages/global`, WebSocket поддерживает только каналы `club:{clubId}`. Mobile: удалены GlobalChatTab, NotificationsTab (orphaned), ChatRealtimeService (orphaned, использовал city-каналы); вкладки заменены на «Личные» / «Клуб» / «Тренер» (Личные и Тренер — заглушки); удалены методы getGlobalChatMessages/sendGlobalMessage. Добавлены l10n ключи tabPersonal, tabClub, tabCoach, cityLabel, personalChatsEmpty, coachMessagesEmpty; удалены tabCity, tabClubs, tabNotifications, globalChatEmpty (ключи noNotifications, notificationsLoadError остались для ProfileNotificationsSection). Подробности — [changes/messages.md](changes/messages.md).
- **Редактирование профиля (backend + mobile):** Реализован экран/форма изменения данных пользователя. Backend: в `UpdateProfileDto` и `UpdateProfileSchema` добавлены опциональные поля `name` (непустая строка, макс. 100 символов) и `avatarUrl` (URL или пустая строка для сброса); обработчик PATCH `/api/users/me/profile` передаёт в `usersRepo.update()` переданные в теле `name` и `avatarUrl`. Mobile: расширен `UsersService.updateProfile(currentCityId?, name?, avatarUrl?)`; добавлен экран `EditProfileScreen` (форма: имя, URL фото, кнопка «Сохранить»); в ProfileScreen — кнопка «Редактировать» в AppBar, переход на `/profile/edit` с передачей `profile.user` через `extra`; маршрут `/profile/edit` в `app.dart`; l10n ключи `editProfileTitle`, `editProfileName`, `editProfilePhotoUrl`, `editProfileSave`, `editProfileNameRequired`, `editProfileEditAction` (en/ru). Город по-прежнему редактируется из основной карточки профиля. Подробности — [changes/users.md](changes/users.md).

- **Полевое тестирование — проблемы с пробежкой и участие в активностях:** Получен фидбек тестировщика: (1) повторный запуск пробежки приводит к ошибке `Exception: Run already started` при том, что экран выглядит как idle (см. `docs/errors-runbook.md`, раздел «звестные открытые»); (2) во время пробежки на карте может не отображаться маршрут, хотя после завершения сессии данные появляются; (3) для событий и клубов кнопки «Присоединиться» и фильтр «Мой клуб» пока не работают, соответствующие методы в `EventsService` и фильтр по clubId в `MapFilters` помечены TODO/placeholder. Ошибки backend в логах за день отсутствуют, проблемы находятся на стороне mobile и неполной реализации фич.

- **Открытые фичи (infra/README.md) — реализация:** Реализованы три блока из раздела «Открытые фичи». (1) **События (join/check-in):** Backend: `POST /api/events/:id/join` и `POST /api/events/:id/check-in` берут userId из auth (Firebase UID → users.id через `findByFirebaseUid`), ошибки возвращаются в формате ADR-0002 `{ code, message, details? }`. Mobile: реализованы `EventsService.joinEvent(eventId)` и `checkInEvent(eventId, longitude, latitude)` (POST на backend, разбор ошибок через ApiException); на EventDetailsScreen кнопка «Присоединиться» вызывает joinEvent, при успехе — перезагрузка события и SnackBar «Вы записаны», при ошибке — SnackBar с сообщением; добавлены i18n ключи eventJoinSuccess, eventJoinError, eventCheckInSuccess, eventCheckInError. (2) **Клубы (присоединение к клубу):** Backend: миграция `006_club_members.sql` (таблица club_members), репозиторий ClubMembersRepository, эндпоинт `POST /api/clubs/:id/join` (auth, проверка дубликата, вставка), ответ 201 с membership; `GET /api/clubs/:id` при наличии auth возвращает isMember и membershipStatus. Mobile: `ClubsService.joinClub(clubId)`, ClubModel расширен полями isMember и membershipStatus; на ClubDetailsScreen кнопка «Присоединиться» и состояние «Вы в клубе» при isMember; i18n: clubJoin, clubJoinSuccess, clubJoinError, clubYouAreMember. (3) **Фильтр «Мой клуб»:** Backend: в GET `/api/users/me/profile` добавлено поле user.primaryClubId (из club_members). Mobile: CurrentClubService (источник правды для текущего клуба), синхронизация с профилем при init(); ProfileUserData.primaryClubId; на EventsScreen добавлен чип «Мой клуб» (filtersMyClub), при выборе подставляется clubId из CurrentClubService и перезапрашивается список событий; после успешного присоединения к клубу вызывается setCurrentClubId. Подробности — [changes/events.md](changes/events.md), [changes/clubs.md](changes/clubs.md), [changes/users.md](changes/users.md), [changes/maps.md](changes/maps.md).

### 2026-02-03
- **Fix: экран результата пробежки (mobile):** справлен расчёт темпа, чтобы не получать формат вида `m:60` при округлении; при ошибке отправки пробежки UI всё равно переводится в состояние `completed` после `stopRun()` (ошибка отображается через SnackBar), флаг `_isSubmitting` сбрасывается в `finally`. Подробности — [changes/runs.md](changes/runs.md).

### 2026-02-02
- **Fix: POST "/" → 404/400 (обработка на backend + mobile):** (1) Backend: добавлен 404-обработчик для неизвестных маршрутов в `app.ts` — возвращает `{ code: "not_found", message: "Route not found: METHOD /path" }`; исправлен формат ошибки 500 с `{ error }` на `{ code, message }` (ADR-0002). (2) Mobile: подтверждено — `RunService.submitRun()` уже использует корректный путь `POST /api/runs`; улучшена обработка ошибок — при 400/404 парсится структурированный ответ backend (`{ code, message }`) и выбрасывается `ApiException`; в `RunScreen` при `ApiException` показывается `message` из ответа вместо `e.toString()`. (3) Тесты: обновлён `app.test.ts` — проверка 404 включает тело `{ code, message }`; добавлен тест POST "/" → 404. Все 55 тестов проходят. Подробности — [changes/runs.md](changes/runs.md).
- **Fix: GET /api/map/data → 401 (обработка на mobile):** в `MapService.getMapData()` добавлено явное распознавание 401 (`ApiException('unauthorized', ...)`); в `MapScreen._loadMapData()` при 401 — попытка обновить токен (`ServiceLocator.refreshAuthToken()`) и повторный запрос; при повторном 401 — редирект на экран входа (`context.go('/login')`). Backend не изменён. Подробности — [changes/maps.md](changes/maps.md).
- **Verified: GET /api/messages/global → 400 (нет cityId) — уже исправлено:** mobile (`global_chat_tab.dart`) не вызывает `/api/messages/global` без `cityId` — проверяет наличие города перед запросом, при отсутствии показывает экран «Укажите город». Backend валидация (400 при отсутствии cityId) корректна. Ошибки 400 в логах — от старых клиентов до применения фикса.
- **Fix: GET /api/me/profile → 401 (потеря Authorization при 302-редиректе):** алиас GET `/api/me/profile` заменён с 302-редиректа на прямой forward запроса в `usersRouter`. 302-редирект терял заголовок `Authorization`, из-за чего клиенты, запрашивающие `/api/me/profile`, получали 401. Теперь оба пути (`/api/me/profile` и `/api/users/me/profile`) обрабатываются одним handler. Подробности — [changes/users.md](changes/users.md).
- **Code review — 3 бага в изменённом коде:** (1) `run_service.dart` — GPS-точки в `submitRun()` отправлялись с полями `lat`/`lon`, но backend (`GpsPointSchema` из `GeoCoordinatesSchema`) ожидает `latitude`/`longitude`; Zod-валидация отклоняла GPS-данные → исправлены имена полей. (2) `global_chat_tab.dart` — fallback `'spb'` в cityId приводил к вызову messages API даже когда город не установлен (noCitySet); убран fallback, при отсутствии города API не вызывается, сразу показывается подсказка «Укажите город». (3) `users.routes.ts` — старые эндпоинты (GET /, GET /:id, POST /, DELETE /me) возвращали ошибки в формате `{ error }` вместо единого `{ code, message }` (ADR-0002); приведены к единому формату. Подробности — [changes/runs.md](changes/runs.md), [changes/messages.md](changes/messages.md), [changes/users.md](changes/users.md).
- **справления по логам и выбор города в профиле:** (1) Ошибка создания run (500, PostgreSQL 22P02): в `runs.routes.ts` userId берётся только из авторизации (`req.authUser.uid` → `findByFirebaseUid`); при отсутствии пользователя или невалидном UUID возвращается 400 в формате API (validation_error / Authentication required). (2) Путь создания пробежки на клиенте подтверждён: POST `/api/runs` в `run_service.dart`, добавлен комментарий о каноническом пути. (3) GET `/api/messages/global`: обязательный query-параметр `cityId`; при отсутствии — 400 с сообщением «cityId is required»; mobile передаёт `cityId` в `getGlobalChatMessages`, при отсутствии города используется дефолт `spb`. (4) Единый путь профиля: добавлен алиас GET `/api/me/profile` → редирект 302 на `/api/users/me/profile`. (5) Выбор города в личном кабинете: миграция `004_users_city_id_varchar` (users.city_id UUID→VARCHAR(64)); PATCH `/api/users/me/profile` с телом `{ currentCityId? }`; в `users.routes` везде используется `req.authUser.uid`; mobile: блок «Город» в ProfileScreen, диалог выбора города, `UsersService.updateProfile`, `ApiClient.patch`, сохранение в CurrentCityService и на backend; l10n ключ `cityNotSelected`. Подробности — [changes/runs.md](changes/runs.md), [changes/users.md](changes/users.md), [changes/messages.md](changes/messages.md), [changes/cities.md](changes/cities.md).
- **Code review — исправление ошибок в мульти-город коммите:** 5 исправлений: (1) `events.repository.ts` — `|| undefined` заменён на `?? undefined` для `participantLimit`, `checkInLongitude`, `checkInLatitude` (баг: значение `0` преобразовывалось в `undefined`); (2) `city_picker_dialog.dart` — 4 хардкод-строки на русском заменены на вызовы `AppLocalizations` (добавлены ключи `cityPickerTitle`, `cityPickerLoadError`, `cityPickerEmpty` в ARB и сгенерированные l10n-файлы); (3) `territories.routes.ts` — mock `cityId: 'city-1'` заменён на `'spb'` для согласованности с конфигом городов; (4) `event_list_item_model_test.dart` — добавлен `cityId` в тестовые JSON и проверки (покрытие нового поля); (5) `map_screen.dart` — удалён `DevRemoteLogger.logError` для штатного события `onMapCreated` (ошибочное использование error-логгера для info-сообщения). Подробности — [changes/cities.md](changes/cities.md).
- **Code review fix — пропущенные i18n строки и deprecated API:** исправлены 6 хардкод-строк на русском, оставшихся после i18n-миграции: `login_screen.dart` (ошибка входа), `run_screen.dart` (текст привязки к тренировке, кнопка «Начать пробежку»), `settings_section.dart` («Выйти из аккаунта»), `club_details_screen.dart` (заголовок «Клуб»), `territory_details_screen.dart` (подпись «Статус»). Заменены на вызовы `AppLocalizations.of(context)!` с существующими ключами из ARB. Также исправлены 3 оставшихся вызова deprecated `withOpacity()` → `Color.fromRGBO()` в `events/widgets/event_card.dart`, `map/widgets/event_card.dart`, `activity_section.dart`. Добавлен пропущенный `const` в `global_chat_tab.dart`. Подробности — [changes/i18n.md](changes/i18n.md).
- **i18n — локализация строк (mobile):** внедрена локализация в мобильном приложении: зависимость `flutter_localizations`, конфигурация l10n (`l10n.yaml`), ARB-файлы `app_en.arb` и `app_ru.arb` в `mobile/l10n/`, генерация `AppLocalizations` в `lib/l10n/`. В `MaterialApp.router` добавлены `localizationsDelegates`, `supportedLocales`, `localeResolutionCallback` (fallback на `ru`). Все пользовательские строки в экранах и виджетах заменены на вызовы `AppLocalizations.of(context)!`. Поддерживаемые локали: русский (ru), английский (en). Сообщения об ошибках (таймаут, сеть, геолокация, пробежка) и коды API локализуются на клиенте. Подробности — [changes/i18n.md](changes/i18n.md).
- **Background GPS — трекинг пробежки в фоне (исправлен):** пакет `background_location:0.13.2` использует устаревший Flutter plugin embedding v1 (`Registrar`) и не компилируется с текущей версией Flutter. Заменён на встроенный foreground service geolocator_android (`AndroidSettings.foregroundNotificationConfig`): при `startTracking(background: true)` geolocator стартует foreground service с уведомлением «Runterra — Run in progress», GPS-трекинг продолжается при сворачивании приложения. Удалены: зависимость background_location из pubspec.yaml, приватный метод `_locationToPosition`, флаг `_isBackgroundTracking`. Разрешения FOREGROUND_SERVICE, FOREGROUND_SERVICE_LOCATION, ACCESS_BACKGROUND_LOCATION, POST_NOTIFICATIONS в AndroidManifest сохранены. `compileSdk` поднят с 35 до 36 (требование geolocator_android, google_sign_in_android, flutter_plugin_android_lifecycle). Подробности — [changes/runs.md](changes/runs.md).
- **Чат — real-time сообщения:** реализована доставка сообщений в реальном времени для городского чата. Backend: миграция 002_messages (таблица messages), модуль messages (entity, DTO, Zod-схема, репозиторий), REST API GET/POST /api/messages/global и GET/POST /api/messages/clubs/:clubId, WebSocket-сервер на пути /ws (auth по query token, подписка на каналы city:{cityId} и club:{clubId}, broadcast при создании сообщения). Mobile: зависимость web_socket_channel, реализация getGlobalChatMessages и sendGlobalMessage в MessagesService, ChatRealtimeService для подписки на WebSocket и стрима новых сообщений, GlobalChatTab — загрузка списка + профиль для cityId, подписка на real-time, поле ввода и отправка сообщений. Подробности — [changes/messages.md](changes/messages.md).
- **Чат — исправления после code review:** исправлен порядок сообщений в GlobalChatTab (reverse начальной загрузки DESC→ASC для корректного отображения с reverse ListView). Backend WebSocket: uid из токена сохраняется на соединении, добавлена валидация канала при subscribe (regex формата, проверка cityId пользователя для city-каналов), добавлен closeChatWs() для graceful shutdown. REST API: добавлена auth-проверка в GET /clubs/:clubId, добавлена parsePagination() с защитой от NaN/отрицательных/превышающих лимит значений. Mobile: _toWsUrl больше не приводит весь URL к lowercase (только схему). Подробности — [changes/messages.md](changes/messages.md).
- **Чат — fix Unhandled Exception user_city_required (logcat):** в GlobalChatTab._fetchData() профиль загружается первым для получения cityId; если город не установлен — messages API не вызывается, вместо красной ошибки показывается дружелюбный экран «Укажите город в профиле» с кнопкой повтора. Устраняет Unhandled Exception в logcat при отсутствии города у пользователя. Подробности — [changes/messages.md](changes/messages.md).
- **Мульти‑город и нормализация карты (backend + mobile):** добавлена полноценная модель города с центром и границами (`center`, `bounds`) и in-memory конфиг городов (СПб) в backend (модуль cities), расширены сущности/DTO событий, клубов и территорий полем `cityId`, добавлена миграция `003_events_city_id` и фильтрация по `cityId`/`clubId` в `/api/events`, `/api/map/data`, `/api/clubs`, `/api/territories`. На мобильном клиенте реализован `CurrentCityService` (кеш + профиль), диалог выбора города, передача `cityId` во все релевантные API-запросы и нормализация карты Yandex MapKit под выбранный город (центр, zoom, ограничения области). Подробности — [changes/cities.md](changes/cities.md), [changes/events.md](changes/events.md), [changes/clubs.md](changes/clubs.md), [changes/territories.md](changes/territories.md), [changes/maps.md](changes/maps.md).

### 2025-01-27
- **Пользователи / профиль:** контракт `club === null`, mock-флаг для заглушек, правила меркателя, ProfileScreen как точка входа. Подробности — [changes/users.md](changes/users.md).
- **События (Events) MVP:** архитектурная основа модуля Events (backend + mobile), типы, DTO, API роутеры, UI компоненты, инварианты статусов. Подробности — [changes/events.md](changes/events.md).
- **Messages MVP табы по 123.md:** табы «Город | Клубы | Уведомления», убраны личные переписки, добавлена вкладка уведомлений, обновлён empty state городского чата. Подробности — [changes/messages.md](changes/messages.md).
- **Run Tab MVP:** реализация MVP вкладки Run согласно 123.md — GPS-трекинг, таймер, три состояния (idle/recording/result), backend модуль runs, API для сохранения пробежек. Подробности — [changes/runs.md](changes/runs.md).
- **Maps MVP:** архитектурная основа карты согласно 123.md — API endpoint для данных карты, модели, сервисы, UI компоненты (территории, события, фильтры, кнопка "Моё местоположение"). Подробности — [changes/maps.md](changes/maps.md).
- **Логирование ошибок пользователей (Sentry):** интеграция Sentry для мониторинга ошибок в mobile (Flutter) и backend (Node.js). Перехват необработанных исключений, явная отправка ошибок из ключевых мест. Подробности — [changes/logging.md](changes/logging.md), [adr/0001-sentry-error-logging.md](adr/0001-sentry-error-logging.md).
- **Настройка Firebase Authentication для Android:** интеграция Firebase Authentication в мобильное приложение — конфигурация Firebase проекта, настройка Gradle, добавление зависимостей, инициализация в коде. Подробности — [changes/firebase-setup.md](changes/firebase-setup.md), [docs/firebase-setup.md](firebase-setup.md).

### 2026-01-31
- **Smoke-тесты (backend + mobile):** добавлены базовые тесты для критичных путей. Backend: Jest + supertest, 18 тестов (health endpoint, auth middleware, все GET /api/*, POST validation). Mobile: flutter_test, 13 тестов (модели EventListItemModel, ClubModel, RunModel — fromJson/toJson, null handling). Команды: `npm test` (backend), `flutter test` (mobile).
- **Кнопка повтора на всех экранах ошибок:** добавлен `onRetry` callback в `ErrorDisplay`, все detail screens и табы сообщений получили возможность перезагрузки данных при ошибке.
- **Миграция карт: Mapbox → Yandex MapKit:** технологический стек карт изменён с Mapbox на Yandex MapKit; причина — лучшая поддержка региона РФ и ценовая политика. Обновлены: pubspec.yaml (yandex_mapkit: ^4.0.0), AndroidManifest.xml (API ключ Яндекс), map_screen.dart (полностью переписан под Yandex API), удалён my_location_button.dart (функционал встроен в map_screen), обновлена документация. Подробности — [changes/maps-yandex.md](changes/maps-yandex.md).
- **Mobile API base URL — облачный backend по умолчанию (debug):** в `api_config.dart` для debug-сборок при отсутствии `--dart-define=API_BASE_URL` используется облачный backend `http://85.208.85.13:3000` (Cloud.ru); для release/production поведение без изменений (https). Подробности — [changes/api-client.md](changes/api-client.md).

### 2026-01-27
- **Backend dev compile fix (Sentry types):** добавлены минимальные TypeScript-типизации для `@sentry/node` в backend, чтобы устранить ошибку TS2307 при локальном запуске без изменения рантайм-поведения. Поведение логирования Sentry осталось без изменений.
 - **Mobile debug build проверен:** выполнена сборка `flutter build apk --debug` для мобильного клиента, критических ошибок нет, APK успешно собран; предупреждения по версии Android Gradle Plugin зафиксированы как технический долг без изменений конфигурации на текущем skeleton-этапе.

### 2026-01-29
- **Backend Firebase auth stub safety:** заглушка проверки Firebase ID токенов в backend помечена как SECURITY STUB, добавлены проверки `NODE_ENV` в `verifyToken`, а также startup-check `assertFirebaseAuthConfigured()` при запуске сервера; в production окружении использование заглушки теперь приводит к немедленной ошибке, чтобы исключить случайный деплой без реальной интеграции Firebase Admin SDK.
 - **Backend API auth middleware:** добавлено глобальное middleware авторизации для всех `/api` роутов, которое ожидает заголовок `Authorization: Bearer <token>` и проксирует проверку в `AuthService.verifyToken()` (FirebaseAuthService stub); в случае отсутствия или невалидного токена эндпоинты возвращают `401 Unauthorized`. Поведение доменных заглушек не изменено, ограничение касается только доступа.
 - **Backend runtime-валидация POST DTO:** подключена техническая runtime-валидация тел запросов для POST-эндпоинтов `activities`, `cities`, `clubs`, `events`, `runs`, `territories`, `users` с использованием Zod-схем на основе существующих `Create*Dto`. Добавлено общее middleware `validateBody(schema)`; при несоответствии формы/типов входных данных backend возвращает `400 Bad Request`, бизнес-логика и доменные инварианты по-прежнему остаются заглушками.
 - **API ошибки и политика валидации:** введён единый конверт ошибок API `{ code; message; details? }`, стандартизирован формат ошибок валидации (`code: \"validation_error\"`, `details.fields[{ field; message; code }]`) для Zod, а также форма ответов `401 Unauthorized` в `authMiddleware`; локализация текстов ошибок остаётся на стороне клиентов. Подробности — [adr/0002-api-error-format-and-validation.md](adr/0002-api-error-format-and-validation.md), [docs/api-errors-and-validation.md](api-errors-and-validation.md).
 - **Mobile FutureBuilder state fix:** экраны деталей (`ActivityDetailsScreen`, `CityDetailsScreen`, `ClubDetailsScreen`, `TerritoryDetailsScreen`, `EventDetailsScreen`) и вкладки сообщений (`GlobalChatTab`, `ClubMessagesTab`, `NotificationsTab`) переведены на `StatefulWidget` с кэшированными `Future` в `initState`, чтобы избежать повторного выполнения HTTP-запросов и заглушек при каждом `rebuild`; бизнес-логика и доменные инварианты не изменены.
 - **Mobile Maps territory radius fix:** радиус placeholder-территорий на карте пересчитан из фиксированных пикселей в экранно-зависимый размер на основе zoom/широты, чтобы приблизительно соответствовать 500 м в реальных метрах и масштабироваться корректно при зуме; доменная логика территорий не изменена, правка затрагивает только визуализацию в `MapScreen`.
 - **Mobile Run duration format fix:** исправлен баг форматирования длительности пробежки в `RunScreen`, из-за которого пробежки дольше 60 минут отображались без часов (например, 1:30:00 как `30:00`); теперь при наличии часов время показывается в формате `H:MM:SS`, иначе `MM:SS`.
 - **Mobile Territories coordinates model dedup:** дублирующийся класс `TerritoryCoordinates` объединён в одну DTO-модель в `territory_model.dart`, `territory_map_model.dart` теперь использует импорт вместо собственной копии; это устраняет конфликт имен при совместном использовании обеих моделей без изменения структуры данных или доменной логики.
 - **Logging refactor (local + dev remote logs):** Sentry интеграция во всех компонентах временно отключена; на backend добавлен простой структурированный логгер и dev-only эндпоинт `POST /dev/log` для приёма удалённых логов от мобильного приложения, в Flutter-клиенте `sentry_flutter` заменён на лёгкий `DevRemoteLogger`, который отправляет технические ошибки на локальный backend. Подробности — [changes/logging.md](changes/logging.md), [adr/0003-local-logging-and-dev-remote-logs.md](adr/0003-local-logging-and-dev-remote-logs.md).
 - **Dev-логи на единый удалённый сервер:** логи в dev отправляются только на внешний лог-сервер (POST /log, http://176.108.255.4:4000); в PROD отправка отключена. Backend: модуль `devLogClient`, при error/warn — fire-and-forget POST на сервер; эндпоинт `/dev/log` удалён. Mobile: `DevRemoteLogger` отправляет только при заданном `DEV_LOG_SERVER` (в prod не задаётся); путь `/log`. Admin: добавлена утилита `sendDevLog` для будущих ошибок API. Единая точка приёма логов — удалённый сервер; чувствительные данные не логируются. Подробности — [changes/logging.md](changes/logging.md).
 - **Mobile Events RefreshIndicator fix:** в `EventsScreen` метод `_refreshEvents()` переведён на возврат `Future<void>` с ожиданием завершения `_fetchEvents()`; `RefreshIndicator.onRefresh` передаётся напрямую `_refreshEvents`, чтобы спиннер pull-to-refresh не исчезал до окончания загрузки. Подробности — [changes/events.md](changes/events.md).
- **Mobile RunService baseUrl fix:** в `RunService` fallback для `ApiClient` заменён с хардкода `http://localhost:3000` на `ApiConfig.getBaseUrl()`, чтобы submit пробежки работал на Android-эмуляторе (10.0.2.2). Подробности — [changes/runs.md](changes/runs.md).
- **firebaseUid утекает в API-ответах (безопасность):** пользовательские эндпоинты `GET /api/users`, `GET /api/users/:id`, `POST /api/users` больше не возвращают сущность User с полем firebaseUid; введён маппинг User → UserViewDto (без firebaseUid) и функция `userToViewDto`. Эндпоинт `GET /api/users/me/profile` уже не раскрывал firebaseUid (ProfileDto.user — отдельный контракт). Подробности — [changes/users.md](changes/users.md).
 - **Единый DB pool (устранение дублирования):** удалён модуль `shared/db`; оставлен единственный источник пула PostgreSQL — `db/client.ts`. Устранены два параллельных пула с коллизией `getDbPool()` и удвоением соединений при импорте обоих. Подробности — [changes/database.md](changes/database.md).
 - **Backend graceful shutdown (DB pool):** в `server.ts` добавлен технический обработчик сигналов `SIGTERM`/`SIGINT`, который при завершении процесса вызывает `closeDbPool()` из `db/client.ts` и логирует результат через `logger`; цель — корректно закрывать пул PostgreSQL и не оставлять висящие соединения при остановке backend-сервера. Подробности — [changes/database.md](changes/database.md).
 - **DB_PASSWORD в production (безопасность):** в production окружении запуск backend без заданного `DB_PASSWORD` приводит к выбросу ошибки в `getDbConfig()` (config/db.ts); дефолт пустой строки сохранён только для не-production. Подробности — [changes/database.md](changes/database.md).
- **Invalid Date при создании run (баг):** в `CreateRunSchema` поля `startedAt` и `endedAt` заменены с `z.string()` на `z.string().datetime()`, чтобы невалидные ISO-строки отклонялись до парсинга; при некорректном формате даты backend возвращает `400` с validation_error вместо сериализации Invalid Date в null. Подробности — [changes/runs.md](changes/runs.md).
- **Mobile API auth (безопасность):** в `ApiClient` добавлена поддержка опционального `authToken` и автоматическая подстановка заголовка `Authorization: Bearer <token>` во все GET и POST запросы; механизм инъекции токена готов, передача токена при создании клиента — ответственность вызывающего кода (например Firebase ID token). Подробности — [changes/api-client.md](changes/api-client.md).
- **Mobile API base URL — HTTPS по умолчанию (безопасность):** в `api_config.dart` по умолчанию используется схема https для base URL; для dev/emulator можно оставить http через `--dart-define=API_BASE_URL=http://10.0.2.2:3000` (или http://localhost:3000). В production не передавать API_BASE_URL — тогда используется https с платформенным хостом. Подробности — [changes/api-client.md](changes/api-client.md).
- **Mobile ApiClient — синглтон и dispose (утечка ресурсов):** `ApiClient` создавал новый `http.Client()` на каждый экран без вызова `close()`, сокеты не закрывались. Введён синглтон через `ApiClient.getInstance(baseUrl:, authToken?)` — один экземпляр на приложение, один `http.Client`. Добавлен метод `dispose()` для закрытия клиента и сброса синглтона (тесты, явный logout). Все экраны и сервисы переведены на `getInstance()`. Подробности — [changes/api-client.md](changes/api-client.md).
- **Mobile ServiceLocator (DI):** внедрён простой ServiceLocator: ApiClient и все API-сервисы создаются один раз в `ServiceLocator.init()` при старте приложения (в `main.dart`). Экраны и вкладки получают сервисы через `ServiceLocator.eventsService`, `ServiceLocator.mapService` и т.д.; дублирование цепочки getBaseUrl → getInstance → new XxxService устранено. Подробности — [changes/api-client.md](changes/api-client.md), [adr/0004-mobile-service-locator-di.md](adr/0004-mobile-service-locator-di.md).
- **Mobile LocationService dispose (утечка памяти):** в `RunService` при создании без инжекции `LocationService` создавался новый экземпляр с `StreamController`, который никогда не освобождался. Добавлен флаг владения `_ownLocationService`; в `cancelRun()` при владении вызывается `_locationService.dispose()` и создаётся новый экземпляр для следующего `startRun()`. Добавлен метод `dispose()` для освобождения при уничтожении RunService (например в тестах). В `ProfileScreen` LocationService не создаётся — используется синглтон из ServiceLocator, вызов dispose там не требуется. Подробности — [changes/runs.md](changes/runs.md).
- **Mobile RunModel (DTO) vs RunSession (UI state):** RunSession — модель состояния трекера (idle/recording/result), без fromJson/toJson, без userId/endedAt/createdAt; duration — Duration, статус — RunSessionStatus (running, completed). Создана отдельная DTO `RunModel` для десериализации backend RunViewDto: id, userId, activityId?, startedAt, endedAt, duration (секунды → Duration), distance, status (RunModelStatus: completed, invalid), createdAt, updatedAt. RunService.submitRun парсит ответ 201 через RunModel.fromJson для согласованности с контрактом backend. Подробности — [changes/runs.md](changes/runs.md).
- **Mobile API error handling (activities, cities, clubs, territories):** в сервисах `ActivitiesService`, `CitiesService`, `ClubsService`, `TerritoriesService` добавлена проверка `response.statusCode` и обработка не-JSON ответов по образцу `EventsService.getEvents()`: при статусе != 200 выбрасывается `Exception`, при ответе не application/json или HTML — `FormatException` с понятным сообщением; парсинг JSON обёрнут в try/catch. Это устраняет FormatException при 404/500 и ответах с HTML. Подробности — [changes/activities.md](changes/activities.md), [changes/cities.md](changes/cities.md), [changes/clubs.md](changes/clubs.md), [changes/territories.md](changes/territories.md).
- **Backend: единый источник конфигурации БД:** конфигурация БД больше не дублируется в env.ts; единственный источник — `config/db.ts`. EnvConfig содержит только `port`; добавлена проверка parseInt на NaN для PORT и DB_PORT; загрузка .env вынесена в явный `loadEnv()` в server.ts. Подробности — [changes/database.md](changes/database.md).
- **Backend: единая auth-абстракция:** удалена дублирующая абстракция auth/service (AuthService, FirebaseAuthService); middleware и код используют только `modules/auth` (AuthProvider, FirebaseAuthProvider, getAuthProvider()). Удалён auth/service.ts; auth/types реэкспортирует типы из modules/auth. Подробности — [changes/auth.md](changes/auth.md).
- **Backend: общий тип координат (latitude, longitude):** введён shared тип `GeoCoordinates` в `shared/types/coordinates.ts`; city, territory, event, run, map используют его (CityCoordinates, TerritoryCoordinates, EventStartLocation, GpsPoint extends GeoCoordinates, MapCoordinates — алиасы). Устранено дублирование в 5 модулях. Подробности — [changes/backend-coordinates.md](changes/backend-coordinates.md).
- **Backend: CORS, rate limiting, express.json limit:** добавлены CORS middleware, rate limit 100 req/min per IP для /api (product_spec 18.1), явный limit для express.json (1mb). Подробности — [changes/backend-security.md](changes/backend-security.md).
- **Backend: env — loadEnv и parseInt NaN:** в env.ts добавлена функция `loadEnv()` для явной загрузки .env (вызов из server.ts); парсинг PORT и DB_PORT с проверкой на NaN и допустимый диапазон. Подробности — [changes/database.md](changes/database.md).
- **Mobile: GoRouter refreshListenable:** статический GoRouter дополнен `refreshListenable: authRefreshNotifier`; при вызове `authRefreshNotifier.refresh()` маршрутизатор обновляется (задел под реакцию на смену auth). Подробности — [changes/mobile-app.md](changes/mobile-app.md).
- **Mobile: EventsScreen — фильтры перезапрашивают данные:** при смене фильтра по дате или «Только открытые» вызывается `_eventsFuture = _fetchEvents()` и список событий перезагружается. Подробности — [changes/events.md](changes/events.md).
- **Open features — участие в активностях и фильтр «Мой клуб» (документация состояния):** Зафиксировано текущее состояние открытых фич из раздела `### Открытые фичи` в `infra/README.md`: (1) участие в событиях (join/check-in) реализовано частично — backend-эндпоинты `POST /api/events/:id/join` и `POST /api/events/:id/check-in` работают через репозиторий и mock-`userId`, но требуют привязки к auth, явной модели участников и приведения к формату ошибок ADR-0002; методы `EventsService.joinEvent/checkInEvent` и кнопка «Присоединиться» в мобильном UI остаются TODO. (2) участие в клубах (join club) на backend ограничено mock-роутером `clubs.routes.ts` без API членства; на мобильном клиенте отсутствует метод `joinClub` и кнопка присоединения на `ClubDetailsScreen`. (3) фильтр «Мой клуб» опирается на уже реализованную поддержку `clubId` в `GET /api/events` и `GET /api/map/data`, но на mobile пока хранится только как placeholder в `MapFilters`/`MapFiltersPanel` и не связан с реальным «моим клубом» пользователя; в `EventsScreen` есть задел под фильтр по клубу, но сам выбор клуба и понятие primary club не реализованы. Текущее состояние и next steps подробно описаны в [infra/README.md](../infra/README.md), ссылки на соответствующие изменения — [changes/events.md](changes/events.md), [changes/clubs.md](changes/clubs.md), [changes/maps.md](changes/maps.md).
- **Docs: infra/README.md — раздел «Открытые фичи»:** Уточнено описание ответа `POST /api/events/:id/join` (перечислены поля `{ success, error?, eventId, participant? }` по фактической реализации) и исправлены относительные ссылки на продуктовую спецификацию и файлы `docs/changes/*` так, чтобы они корректно разрешались из директории `infra/`. Поведение backend и mobile не изменилось, обновлена только документация.
- **Mobile: исправление средних проблем (36-50):** исправлены проблемы навигации (context.go → context.push для сохранения back stack), оптимизирован O(n²) пересчёт дистанции в RunScreen, синхронизированы _onMapCreated и _loadMapData в MapScreen, исправлена обработка ошибок GPS permissions, убраны хардкоды, вынесена общая обработка ошибок в ErrorDisplay виджет, добавлен парсинг clubId в TerritoryModel, исправлены unsafe as int casts во всех моделях, исправлен api_config.dart для совместимости с Flutter Web. Подробности — [changes/mobile-fixes.md](changes/mobile-fixes.md).
- **Backend: исправление низких проблем (L-1–L-10):** исправлены технические проблемы кода: префикс _ для неиспользуемых параметров в error handler, адрес прослушивания сервера зависит от окружения (0.0.0.0 только в dev), добавлено приведение типов для req.query в events/map routes, роутеры переведены на barrel exports модулей, интерфейс Notification переименован в UserNotification для избежания конфликта с Web API, исправлен дублирующийся re-export в auth barrel, переименовано поле isMercantile → isMercenary во всех файлах. Подробности — [changes/backend-low-issues.md](changes/backend-low-issues.md).
- **Mobile: исправление низких проблем (L-11–L-27):** исправлены технические проблемы кода: убраны бесполезные try/catch с rethrow в ApiClient, заменены deprecated withOpacity() на Color.fromRGBO, исправлена иммутабельность RunSession.gpsPoints, добавлены TODO комментарии для i18n/Equatable/retry, исправлены фильтры событий (отправка query параметров), заменено ручное форматирование дат на DateFormat, вынесен общий виджет NotificationItem для устранения дублирования, добавлен copyWith для MapFilters, удалён неиспользуемый метод _showEventCard. Подробности — [changes/mobile-low-issues-l11-l27.md](changes/mobile-low-issues-l11-l27.md).

## 2026-02-26
- Fixed 'Unsupported class file major version 69' error by updating settings.json so VS Code Java extensions use the Android Studio Java 21 distribution, and updated Gradle wrapper to 8.10.2 for mobile and wear modules.
