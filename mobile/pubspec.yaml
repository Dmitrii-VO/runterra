name: runterra
description: Runterra mobile application
publish_to: 'none'
version: 1.0.18+321

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  yandex_mapkit: 4.2.1
  http: ^1.1.0
  geolocator: ^13.0.0
  go_router: ^14.0.0
  intl: ">=0.19.0 <=0.20.2"
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  firebase_analytics: ^11.0.0
  google_sign_in: ^6.2.1
  web_socket_channel: ^2.4.0
  shared_preferences: ^2.2.2
  watch_connectivity: ^0.2.4
  flutter_tts: ^4.2.5
  package_info_plus: ^8.0.0
  url_launcher: ^6.3.0
  firebase_storage: ^12.0.0
  image_picker: ^1.1.2
  pedometer: ^4.0.2
  permission_handler: ^11.3.0
  wakelock_plus: ^1.2.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
